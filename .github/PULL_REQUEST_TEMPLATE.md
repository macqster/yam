## Summary

<!-- What changed and why. If this PR grows past its original scope (more
commits landed after opening it), update this section to match — a stale
description that only covers the first commit is a known recurring gap. -->

## Test plan

- [ ] `bash scripts/verify.sh` passes
- [ ] New/changed behavior has a regression test
- [ ] Docs and `docs/LOG.md` updated in the same change if behavior changed
