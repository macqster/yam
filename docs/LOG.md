# YAM Log

<!-- cspell:ignore ntrospect0 -->

This file is append-only and historical only; current rules live in the active docs.

Logging rule:

- Prefer day headers in the form `## YYYY-MM-DD` for grouping.
- Keep day sections in reverse-chronological order, with the most recent day at the top of the log.
- Prefer inline timestamps in the form ``HH:MM`` for entries within a day.
- Within a day section, keep entries in chronological order when the sequence is known.
- Use the local system time for new entries. In the current session that means `Europe/Paris`, including summer-time shifts such as `CEST` / `UTC+02:00` when applicable.
- New entries should use a full date and time stamp whenever practical, especially when the exact sequence within a day matters.
- Existing historical entries are kept intact unless a future maintenance pass explicitly needs to refine them.
- Prefer append-only additions over rewriting older lines.

## 2026-07-22 - Critical re-inspection and docs reconciliation

- `08:34 CEST` re-verified the prior session's handoff claims from scratch rather than trusting them: reviewed `d647343` (the user's own commit) in full and confirmed it genuinely resolves the weather-test/production-path tension the handoff flagged; ran `bash scripts/verify.sh` fresh (267/267, matches the current claim, not the stale 258 figure that had been left in `docs/greenhouse-roadmap.md`'s prose); ran `cargo clippy --all-targets --all-features -- -D warnings` and `cargo check --all-targets` clean; grepped every `.vines` call site to confirm the `FloraInstance` migration left zero direct references to the old bespoke field; traced every `match` on `WorldKind` to confirm no wildcard arm could silently mishandle `Greenhouse`; and confirmed the camera-does-not-recenter-on-world-switch scope decision is consistent (true for `Sandbox` too, not greenhouse-specific).
- `08:34 CEST` fixed a same-day self-contradiction in `docs/greenhouse-roadmap.md`: the "0.4 Readiness Snapshot" prose said `258` tests while the Gate Checklist table two sections later said `267` for the same date; corrected the prose to `267`. Also fixed a stale stop-condition line still naming `FloraState::vines` as a field after the `FloraInstance` migration renamed it to `organisms`.
- `08:34 CEST` fixed staleness in `docs/vines.md`, which this session's predecessor never touched: three places described anchor resolution as going through "the active compatibility layer" — a reference to `scene::coords`, which was retired 2026-07-21. Updated to name `core::spatial::SpatialAnchorLookup` directly, matching `docs/scene-model.md`'s already-correct description. Also added the `FloraInstance` family-store shape to the Current Status/Ownership Contract sections, which previously described vine storage as "future world/flora state" even though it has been live since 2026-07-21.
- `08:34 CEST` pruned `docs/audit.md`'s Active Risk Notes against the file's own stated rule ("keep resolved detail in `docs/LOG.md`... rather than re-accumulating it here"), which a prior pass had been violating by appending full incident narration (`**Fixed**`/`**Superseded**` paragraphs) instead of short current-status lines. Collapsed roughly a dozen multi-sentence historical write-ups (flora storage migration, `fields.rs` panic sweep, weather-worker fix, dependency updates, clippy fixes, `scene::coords` retirement told from four separate angles, several near-duplicate greenhouse-roadmap-ingestion bullets) into one-line current-state statements pointing back at this log for the story. Also fixed a literal copy-paste bug: two duplicate `evidence:` lines at the end of the file.
- `08:34 CEST` reviewed `AGENTS.md` and both repo-local skills (`skills/yam-maintenance`, `skills/yam-architecture-review`) for staleness: found none — both stay pointer-heavy and never named the retired `scene::coords` or the old `vines` field shape, so nothing there could go stale. However, neither skill nor `docs/hygiene.md` documented the `tmux`-based verification method the prior session improvised for this TUI app (there is no browser-based "start the dev server and look at it" equivalent here), nor two other recurring disciplines from that session: reproducing a bug before trusting its regression test, and preferring non-wildcard matches on core enums for compiler-driven exhaustiveness.
- `08:50 CEST` codified the three lessons above: added the reproduce-before-trusting-a-test discipline, the non-wildcard-core-enum-match preference, and the `tmux`-based TUI verification method (with the concrete `tmux new-session -d` / `send-keys` / `capture-pane -p` invocation) as new rules in `docs/hygiene.md`. Added a staged-scope-discussion rule to `AGENTS.md`'s Operating Rules (propose the narrowest next slice, confirm, build, repeat) rather than discussing a whole feature's scope up front, generalizing the pattern this session's predecessor used for the `WorldKind::Greenhouse` work. `bash scripts/check-docs.sh` clean after each change.
- `09:06 CEST` split all uncommitted work into 4 logical commits rather than one mega-commit, splitting `src/core/world.rs`, `.cspell.json`, and `docs/LOG.md` at the hunk level (via `git apply --cached` on hand-extracted patches, and `git stash push --keep-index` to check out and independently verify each intermediate state) so the flora-storage/growth-dispatch/species-profile work and the `WorldKind::Greenhouse` enum-plus-render-layer work landed as separate, individually-compiling, individually-test-green commits rather than one entangled diff: `7cc3e80` (HighGrow reference analysis), `03d14d7` (flora storage/growth dispatch/species-profile, 262/262 tests), `90fa2a1` (`WorldKind::Greenhouse` + `GreenhouseLayer`, 267/267 tests), `41c1a55` (this doc-reconciliation pass). Pushed all 5 commits that hadn't yet reached `origin` (`d647343` plus the 4 above) to `origin/consolidate-spatial-and-harden-verification` as a plain fast-forward (0 behind, nothing rewritten). Updated PR #5's title and description (previously stale, describing only `4f5c6b4`) to account for the full commit set now on the branch. `bash scripts/verify.sh` clean at every intermediate and final state.
- `09:23 CEST` reviewed PR #5 in full via `gh pr diff` (not local `git diff`, per the review skill's instructions) rather than re-reading only the parts already touched this session; independently re-traced `WorldKind` exhaustiveness, the `fields.rs` bounds/repair fix, and the `FloraInstance` migration directly against the diff, and confirmed no functional issues. Found the dependency-graph note in `docs/audit.md` understated the 2026-07-21 `ratatui` "patch" bump: `git diff main...HEAD -- Cargo.lock` shows 9 genuinely new transitive crates (`approx`, `by_address`, `critical-section`, `fast-srgb8`, `libm`, `palette`, `palette_derive`, `ratatui-termina`, `termina`), not just a version bump. Expanded that note with the concrete crate list and confirmed via `cargo tree -d` and `cargo tree -i ratatui-termina` that none of it introduces a new duplicate-version conflict or ships an unused backend into the binary. Added `libm`/`srgb` to `.cspell.json` for the new note. The other review finding (a fastfetch-config path fix bundled into an otherwise-unrelated commit, `4f5c6b4`) was already logged at the time it landed and isn't worth a history rewrite now that it's pushed, so left as-is.
- `09:37 CEST` before merging PR #5, closed the "no CI, only local `bash scripts/verify.sh`" gap flagged during the review above: added `.github/workflows/verify.yml` (installs the Rust toolchain from `rust-toolchain.toml`, `npm install -g markdownlint-cli2 cspell` so the docs checks run for real instead of silently no-op'ing per `run_if_available`, then `bash scripts/verify.sh`) triggered on every push and pull request targeting `main`. Documented the new gate in `AGENTS.md`, `docs/hygiene.md` (alongside the existing opt-in local `pre-push` hook, contrasting always-on CI with the per-clone opt-in), and `docs/release-model.md` (also loosened its "land directly on `main`" wording to acknowledge the short-lived-branch-plus-PR shape this very pass used). Pushing the workflow file itself required the git credential's OAuth token to carry the `workflow` scope, which it initially lacked (`gh auth refresh -s workflow` needed a second, fully-completed interactive run before the scope actually took on the token in use — verified directly against `GET /user`'s `x-oauth-scopes` header rather than trusting `gh auth status`'s cached view, which lagged behind).
- `09:53 CEST` watched the new `Verify` workflow run green on PR #5 (`gh run watch`, 2m24s, all steps passed) before touching branch protection, rather than configuring the required check against an unproven workflow. Configured branch protection on `main` via the GitHub API: required status check `verify` (strict, i.e. the PR branch must be up to date), no force pushes, no branch deletions, required conversation resolution; left `enforce_admins: false` and no required PR-review count, since this remains a solo-maintained repo and the goal was a real automated gate, not process overhead. Merged PR #5 as a genuine merge commit (`1a5b5e3`, not squash or fast-forward-only) once the PR showed `mergeStateStatus: CLEAN` with the new check green, and let `gh pr merge --delete-branch` remove `consolidate-spatial-and-harden-verification` both on `origin` and locally. Re-ran `bash scripts/verify.sh` directly on the post-merge `main` tip as an independent final check, not just trusting the PR's own green state: 267/267 tests, clean.
- `10:04 CEST` acted on a retrospective's concrete recommendations one by one, verifying each against the GitHub API rather than assuming the call succeeded: enabled `delete_branch_on_merge` (every merge up to now needed a manual `--delete-branch`); disabled `allow_squash_merge`/`allow_rebase_merge` so `allow_merge_commit` is now the repo's only merge method, matching the convention just used for PR #5 rather than leaving it a per-PR judgment call; enabled Dependabot security updates (needed enabling `vulnerability-alerts` first — the API rejected `automated-security-fixes` with a 422 until that was on) on top of the existing manual dependency-freshness checks in `docs/audit.md`; added `.github/PULL_REQUEST_TEMPLATE.md` with a short reminder to keep a PR's description in sync if it grows past its original commit set, directly naming the gap PR #5's stale title/body fell into; and wrote up the hunk-level `git apply --cached` / `git stash push --keep-index` commit-splitting technique as a repeatable recipe in `docs/hygiene.md` instead of leaving it as one-off tacit knowledge. Documented the merge-method restriction in `docs/release-model.md` and the Dependabot change in `docs/audit.md`. `bash scripts/verify.sh` clean throughout.
- `10:20 CEST` did a full documentation review pass, README down: read every active doc top to bottom rather than spot-checking. Found and fixed a recurring pattern — `README.md`'s `worlds` line, `docs/architecture.md` (four spots, one directly contradicting its own Active Layers section further down the same file), `docs/rendering.md`'s Layer Order list, `docs/scene-model.md`, `docs/glossary.md`'s greenhouse entry, and `TODO.md`'s Flora Runtime section (contradicting its own already-correct Section 3) all still described `WorldKind::Greenhouse` as purely future, when it landed as a real selectable world earlier this session. Updated all six to distinguish "the world now exists (minimal read-only render)" from "rooms/labs/multi-species growth remain future." `bash scripts/verify.sh` clean after the batch.
- `10:26 CEST` refreshed `docs/chatgpt-0.4-source-pack/` per its own manifest's refresh rule (flora storage direction, greenhouse/world contract, and world-selection contract all materially changed since the `2026-05-31` export): `project-brief.md` and `architecture-constraints.md` were still listing only `MainScene`/`Sandbox` as selectable worlds and describing `scene::coords` as "compatibility-only" rather than retired; `readiness-and-gates.md` had the most drift, still framing flora-store shape as an open decision and `FloraState` as vine-shaped when both are now locked, and still listing a selectable greenhouse world as a first-pass non-goal without noting it already landed (deliberately, after the data-ownership gates, not as a first move). Updated all four files plus `greenhouse-brief.md`'s opening line, and bumped the manifest's export date/commit to this session's `HEAD` (`c49d4f0`). `repo-workflow-brief.md`, `chatgpt-usage-guide.md`, and the pack's own `README.md` needed no changes.
- `10:28 CEST` deleted `docs/REFERENCE_ARCHIVE.md`: it pointed at 19 filenames inside a `yam-rust_debugging_260424-2009_dump` folder that does not exist anywhere in this repo or its git history (confirmed via `git log --all --diff-filter=D` finding nothing). `docs/archive/todo_review.md`'s `sediment://` citation markers referencing the same filename confirm this was ingested ChatGPT-conversation context, never actually committed here — the folder was local/external from the start, not something later deleted. `docs/archive/README.md` already indexes the real, current versions of the same-dated (2026-04-24/25) material under `docs/archive/reports/`, so nothing of value was lost. Removed the matching link from `docs/README.md`'s Archive Entry Points. Left `docs/LOG.md`'s own historical entry recording this file's original addition untouched, per the append-only rule. Separately, added `soft-line-atlas.md`, `palette-reference.md`, and `palette-sheet-reference.md` to `docs/README.md`'s index — all three exist and are actively referenced from other docs, but were missing from the map itself.
- `10:29 CEST` relativized 8 markdown links across `docs/hero-cache.md` (1) and `docs/loading-screen.md` (7) that used an absolute local path (`/Users/mcq/_git/yam/src/...`) instead of a repo-relative one (`../src/...`) — the links happened to work on this machine but would break for anyone else, or on this machine if the repo ever moved. Verified all 9 target files exist at their relative paths before committing. `docs/main-scene-scaffold.md`'s and `docs/soft-line-atlas.md`'s references to personal, non-repo source files (a Downloads brainstorming note; handwritten line-sample study files) are a related but distinct issue — plain-text mentions rather than broken links — left for a follow-up pass.
- `11:55 CEST` closed that follow-up: `docs/main-scene-scaffold.md`'s coordinate-source note framed a personal `/Users/mcq/Downloads/yam_tree_scaffold_brainstroming.md` file as "an active reference," but the note's own Coordinate Reference table already fully distills that source — reworded both spots to state the table is now the complete, self-contained record, and that the original file was never committed and shouldn't be treated as available going forward. `docs/soft-line-atlas.md`'s Cross-reference list got a one-line provenance clarification (personal, non-repo source material, not required to use the atlas) rather than editing every narrative mention of "the handwritten study files," since those read fine as historical color and don't claim the files are currently available. This closes out the full documentation review pass started earlier this session — all 11 findings from that review are now addressed. `bash scripts/verify.sh` clean (267/267).

## 2026-07-21 - Audit-driven cleanup pass

- `14:46 CEST` fixed a log-hygiene regression: an earlier uncommitted `2026-06-18 - Palette authority cleanup` entry had been appended after the `2026-05-13` sections instead of placed in reverse-chronological order; moved it to the top of the log, above `2026-06-05`.
- `14:46 CEST` refreshed `docs/audit.md`'s stale `Last reviewed: 2026-05-31` stamp to `2026-06-18` to match the file's actual last substantive content (the `2026-06-05` scaffold/sandbox risk notes), and added two new dated risk notes: `Cargo.lock` now carries 12 transitively duplicated crates (`bitflags`, `foldhash`, `getrandom`, `hashbrown`, `nom`, `r-efi`, `rand`, `rand_core`, `syn`, `thiserror`, `thiserror-impl`, `wit-bindgen`) since the last readiness gate's "no duplicated dependency graph" reading, and a brace-aware re-scan found 19 non-test `.expect()`/`unreachable!()` call sites (not the previously assumed one) across `runtime.rs`, `render/hero.rs`, `render/fonts.rs`, and `weather/atlas.rs`, all indexing compile-time-fixed data and read as safe-by-construction.
- `14:46 CEST` closed the one panic-risk site that wasn't safe-by-construction: `GreenhouseState::active_room()` (`src/core/greenhouse.rs`) now returns `Option<&GreenhouseRoom>` instead of `.expect()`-panicking, because its resolution invariant is only checked in `GreenhouseState::new` while `rooms`/`active_room_id` stay public and mutable afterward. Updated the four in-module test call sites accordingly.
- `14:46 CEST` verification: the four `rg`-based architecture guardrails in `scripts/check.sh` (core/scene isolation, systems/scene-render-ui-terminal isolation, `project_world_to_screen` and `scene::coords` compatibility-code isolation) all pass, and `scripts/check-docs.sh` passes clean. `cargo fmt`/`clippy`/`check`/`test` could not run in this sandbox (no Rust toolchain, and outbound network to rustup/crates.io stayed blocked at the sandbox proxy layer even after network egress was reportedly enabled) — `bash scripts/verify.sh` still needs to run locally before this pass is trusted for the Rust source edit.
- `14:46 CEST` scanned for broader cleanup/optimization opportunities (`.clone()`/`.to_string()` density, `TODO`/`FIXME` markers, dead code): the source tree stayed lean (roughly two dozen non-test `.clone()` calls total, no TODO/FIXME/XXX markers), so no further source changes were made this pass beyond the greenhouse fix above.
- `15:20 CEST` checked tooling/infrastructure freshness: `rust-toolchain.toml` (`channel = "stable"`) and the unversioned `Brewfile` formulas already track latest by design. `crates.io`/`sh.rustup.rs` stayed blocked from the sandbox, but `docs.rs` version pages were reachable and gave real current-release data: `ratatui` 0.30.0 → 0.30.2, `chrono` 0.4.44 → 0.4.45, `serde` 1.0.228 → 1.0.229, `serde_json` 1.0.150 → 1.0.151, `unicode-segmentation` 1.13.2 → 1.13.3, and `tachyonfx` 0.25.0 → 0.25.1 all have newer patch releases already inside the existing `Cargo.toml` ranges (no manifest edit needed, just a lockfile refresh); `crossterm`, `image`, `tempfile`, `unicode-width`, `figlet-rs`, and `ansi-to-tui` are already current. No `Cargo.lock` edit was made without `cargo` available to verify it. `docs/audit.md` records the specifics and the local command to run (`cargo update -p ratatui -p chrono -p serde -p serde_json -p unicode-segmentation -p tachyonfx && cargo test`). A discretionary Rust 2024 edition migration is available but not required and was left alone.
- `16:05 CEST` this session had a real Rust toolchain available (unlike the earlier sandboxed passes above), so verified the previous session's `greenhouse.rs` `.expect()` fix directly: `cargo fmt` found one formatting drift in the manual edit (fixed), `cargo check --all-targets` and the full `cargo test` suite (262 passed) were clean, and `active_room()`'s signature change was confirmed to have zero external callers. `cargo clippy --all-targets -- -D warnings` failed with 4 pre-existing errors unrelated to the greenhouse edit; confirmed pre-existing via `git stash` + re-run on unmodified `main`. One test (`ui::state::tests::failed_weather_refresh_marks_existing_snapshot_stale`) failed on a full-suite run but passed both in isolation and on a full rerun — pre-existing test-order flakiness, not caused by any edit this session. Also cleared a stale 0-byte `.git/index.lock` (held only by a macOS indexing service's read handle, not an active git process).
- `17:40 CEST` retired the `scene::coords` compatibility module (`src/scene/coords.rs`) as the next real slice of spatial-relation consolidation: a repo-wide audit (two independent greps, `crate::scene::coords` and the bare `coords` token) found zero call sites outside the module's own tests, so per `docs/architecture.md`'s own migration plan ("retire the old helper paths once tests and docs are all pointing at the new canonical layer") the module was deleted rather than kept as dead weight. Ported the one non-redundant test (`world_origin_and_quadrants_are_signed_from_zero`, a datum/quadrant-sign sanity check) into `src/core/spatial.rs`; the rest of `coords.rs`'s tests were already redundant with direct `core::spatial`/`scene::entity` test coverage. Removed `pub mod coords;` from `src/scene/mod.rs` and the two now-vacuous `scripts/check.sh` guards (`project_world_to_screen` isolation, `crate::scene::coords` isolation) that had nothing left to guard. Test count dropped from 262 to 255, the expected delta (8 removed, 1 ported). Updated `docs/architecture.md`, `docs/scene-model.md`, `docs/rendering.md`, `docs/hygiene.md`, `docs/greenhouse-roadmap.md`, and `docs/audit.md` to describe the retirement instead of an in-progress migration, and removed the now-closed `scene::coords`-specific items from `TODO.md`'s Spatial Relation Layer section (kept the standing invariants that don't reference the retired module). Left `vines.md`'s dated `2026-05-05` Phase-0 snapshot untouched as historical record.
- `18:10 CEST` fixed the pre-existing clippy debt found at `16:05 CEST`: 3 `items-after-test-module` errors (`src/render/chafa.rs:99`, `src/render/hero.rs:298`, `src/scene/layers/hero_layer.rs:193`) were each fixed by moving that file's `#[cfg(test)] mod tests` block to the end of the file — a pure reorder, verified byte-identical non-blank content via `diff` before and after each move — and the 1 `useless-conversion` error (`src/runtime.rs:459`) was fixed by removing a redundant `.into()` on an already-`Duration` value. `cargo fmt`, `cargo clippy --all-targets -- -D warnings`, `cargo check --all-targets`, and `cargo test` (255 passed, the one flaky weather test passed on this run) are all now clean. Also found and fixed a `cspell` failure blocking `scripts/check-docs.sh`: `serde` (introduced by an earlier session's uncommitted dependency-freshness note) wasn't in the project dictionary, so added it to `.cspell.json`. `bash scripts/verify.sh` now passes end-to-end for the first time this session, satisfying the "verification green" gate `TODO.md` and `docs/greenhouse-roadmap.md` require before broad feature work.
- `18:15 CEST` corrected a stale audit note now that a real `cargo tree -d` was available: the `15:20`-adjacent note (from an earlier toolless-sandbox pass) claiming 12 transitively duplicated crates was inferred from manually reading `Cargo.lock` text; a live `cargo tree -d` run reports "nothing to print" — a genuinely clean dependency graph, matching the original 2026-05-31 reading. `docs/audit.md` now flags the manual-grep inference as superseded and inaccurate rather than repeating it as current risk. Refreshed `docs/greenhouse-roadmap.md`'s 0.4 Readiness Snapshot "Last checked" and test-count figures (245 → 255) for the rows re-verified directly this session (baseline verification, spatial ownership, dependency inventory).
- `18:35 CEST` looked for a bounded next slice under the audit's priority #2 (hero-rendering pipeline hardening) and closed a previously-deferred verification instead of building new speculative mechanics: re-traced all 19 non-test `.expect()`/`unreachable!()` sites flagged earlier today (same locations, brace-aware re-scan confirmed no drift) with a real compiler rather than text inference — `runtime.rs:50`'s `unreachable!()` is structurally guarded by `UiState::effect_phase()`'s exhaustive match; `hero.rs:92`'s frame-list non-emptiness is guaranteed by both `Hero` constructors, and a repo-wide grep confirmed `Hero` has no external struct-literal construction and `frames` is never mutated outside `hero.rs`; `fonts.rs:80` only panics if three independent parses of a compile-time-embedded bundled font all fail; and the `weather/atlas.rs` sites index fixed-width compile-time sprite constants covered by existing atlas tests. All 19 remain genuinely safe-by-construction, unlike the `GreenhouseState::active_room()` case fixed earlier today where the invariant actually was reachable through live mutation. No source changes made; `docs/audit.md` records the closure. Considered the offline hero-frame-compiler / `CellGrid`-authoring direction documented in `docs/rendering.md`/`docs/architecture.md` for this same priority, but that is exactly the "large mechanic" `AGENTS.md` says prep work should not build without an explicit ask, so left it as documented-but-deferred rather than starting it.
- `20:05 CEST` fixed the real root cause of the intermittently-failing `ui::state::tests::failed_weather_refresh_marks_existing_snapshot_stale` (previously logged as tolerated "test-order flakiness"): both weather refresh tests actually shelled out to `curl` against the live `wttr.in` endpoint through `WttrInWeatherProvider` via `UiState::refresh_weather_now()`, so a slow/blocked/rate-limited network call under parallel `cargo test` execution would make the real production fallback path kick in (`apply_weather_refresh_result`'s `Err` branch seeds a `StaticWeatherProvider` snapshot explicitly marked `stale: true` when there's no existing snapshot yet) — exactly reproducing the observed `assert!(!fresh.stale)` failure. Fixed by adding a `spawn_weather_snapshot_fetch(location)` seam with `#[cfg(test)]`/`#[cfg(not(test))]` variants (matching the existing `default_hero()` pattern in the same file): test builds resolve synchronously against the deterministic `StaticWeatherProvider`, production builds keep the real threaded `WttrInWeatherProvider` fetch unchanged. Required gating the `thread` import and the `WttrInWeatherProvider` struct itself (`#[cfg_attr(test, allow(dead_code))]`, same pattern as `Hero::new()`) to avoid unused-import/dead-code lint failures under test cfg. Verified with three consecutive full-suite runs (255/255 each, previously failed roughly every other run) plus `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` clean.
- `20:20 CEST` ran the dependency update `docs/audit.md` had queued: `cargo update -p ratatui -p chrono -p serde -p serde_json -p unicode-segmentation -p tachyonfx`. Five of six bumped as expected; `serde` was deliberately left at `1.0.228` (verified `1.0.229` is available and resolvable via `--dry-run`, but bumping it pulls in a new `syn 3.0.2` on top of the pre-existing `syn` 1.x/2.x pair that's confined to `build.rs`'s `built` crate) — chose not to deepen that drift for a patch-only bump with no functional need. The `ratatui` bump introduced one genuinely new normal-graph duplicate, `hashbrown` `0.16.1`/`0.17.1` (via `kasuari` vs. the newly-bumped `lru` 0.18.1), confirmed before/after with `cargo tree -d` and `git diff Cargo.lock` — upstream drift inside `ratatui`'s own tree, not actionable from `yam-rust`'s `Cargo.toml`. `cargo build --release`, `bash scripts/verify.sh`, and `./target/release/yam-rust --identity` all confirmed clean after the update. `docs/audit.md`'s dependency-freshness and dependency-graph notes were updated with the verified outcome in place of the queued recommendation.
- `20:40 CEST` swept for the same shape of risk that caused the `GreenhouseState::active_room()` bug (an invariant enforced only at construction while the backing fields stay `pub` and mutable forever after) across the rest of the source tree: non-test `.unwrap()`/`panic!()` usage (found only two lines, both already inside a test module), every other find-by-id resolver (`core/world.rs`, `core/guide.rs` — all already return `Option`), and every direct grid/array index in production code. Found one real, previously-unnoticed instance: `systems::fields::update_fields()` indexed `world.fields.density[idx]` from `world.grid.index(entity.x, entity.y)` with no bounds-check against the entity's actual position — a panic risk on the main per-frame tick path, currently latent only because `world.entities` is always empty in production today (nothing pushes to it yet), exactly like the greenhouse case before its fix. Fixed by skipping out-of-bounds entities before indexing; added two regression tests, and confirmed the fix is load-bearing by temporarily reverting the guard and reproducing the exact panic (`index out of bounds: the len is 36000 but the index is 19726035`) before restoring it. Every other direct grid/array index found in production code (`render/compositor.rs`, `render/mask.rs`, `render/drawing.rs`, `scene/layers/field_layer.rs`) was already bounds-checked immediately before use. `bash scripts/verify.sh` passes with 257 tests (255 + 2 new).
- `20:54 CEST` closed two follow-up review gaps: `systems::fields::update_fields()` now normalizes all three public field buffers to the grid area before clearing or indexing them, so independently mutated grid/field state cannot reintroduce a panic; and weather tests now inject deterministic fetch results while traversing the same spawned worker/channel path as production instead of using a separate synchronous implementation. Added the mismatched-buffer regression, routed the stale-weather failure test through the worker, aligned the weather contract, audit, and greenhouse readiness snapshot, and recorded Ratatui's verified upstream `hashbrown` `0.16.1`/`0.17.1` duplicate instead of claiming a clean dependency graph. Focused tests passed, `cargo tree -d` confirmed the documented dependency paths, and `bash scripts/verify.sh` passed with 258 tests.
- `21:00 CEST` formalized two process conventions in `docs/hygiene.md` distilled from repeated mistakes this session: findings produced without a working Rust toolchain must now be explicitly labeled inferred/unverified rather than stated as fact (this session found two prior instances where toolless inference from `Cargo.lock`/`docs/*.md` text turned out wrong once checked with real `cargo`), and dated readiness snapshots must record which specific claims were re-verified in a given pass versus carried forward, instead of bumping a whole date stamp for a partial check.
- `21:05 CEST` prepared (but did not activate) an opt-in `pre-push` git hook at `scripts/git-hooks/pre-push` that runs `scripts/verify.sh` before every push, so the "verification green" gate can't silently drift out of green again the way it did going into this session. Left disabled by default and documented as an explicit one-time opt-in (`git config core.hooksPath scripts/git-hooks`) rather than activated automatically, since wiring `core.hooksPath` changes standing behavior for every future push and adds real wall-clock time to it — that tradeoff belongs to the user's choice, not a default. Verified the hook script itself runs correctly (`bash scripts/git-hooks/pre-push`, full pass).
- `22:10 CEST` distilled HighGrow 4.20 (already credited in `README.md`'s acknowledgements) into a proper reference-analysis doc: extracted its bundled `.chm` help manual (via Homebrew's `chmlib`/`extract_chmLib`, after installing the game locally through Wine/Sikarugir for inspection only, never run) plus readable strings from its `Comments.dll`/`Robbie.dll` resource files, then distilled the mechanics into vocabulary comparable to YAM's own greenhouse model. Filed as `docs/archive/greenhouse-brainstorming/highgrow_reference_analysis.md`, following the same non-binding/promotion-required convention as the existing brainstorming files in that directory (confirmed `docs/archive/**` stays exempt from `cspell`/`check-docs.sh`). Notable transferable patterns: a room/fixture structure that maps directly onto `core::greenhouse`'s room/zone/fixture vocabulary; a reusable "advantages/disadvantages/to-improve" soil-archetype data shape; a literal branch-on-prune growth-graph rule (clipping a tip spawns two new tips at the nearest leaf axil) that's a concrete precedent for the graph-plus-segment flora model already described in `docs/architecture.md`; and a deterministic once-per-day batch catch-up tick (idempotent per calendar day, no live background process needed) as a proven shape for any future greenhouse tick model that must survive the app being closed. Added a short cross-reference pointer to it from `docs/greenhouse-roadmap.md`'s brainstorming-reference section. `bash scripts/check-docs.sh` confirmed clean after the addition.

## 2026-07-21 (continued) - Greenhouse-expansion prep: flora storage lock-in and WorldState attachment

- `23:15 CEST` locked the flora-storage decision the roadmap had left as "decision-biased": `FloraState` now stores an enum-backed `FloraInstance` family store (`Vine(VineInstance)`, the sole variant until a second family lands) in a unified `organisms: Vec<FloraInstance>`, replacing the bespoke `vines: Vec<VineInstance>` field. Added `vines()`/`vines_mut()` read/write accessor views (returning `Vec<&VineInstance>`/`Vec<&mut VineInstance>` so existing indexed call sites needed only a mechanical `()` insertion, not a rewrite) and `push_vine(...)`. Migrated every call site: `systems::growth::run_growth`, `systems::aging::run_aging`, `scene::layers::vine_layer`, `scene::layers::debug_layer`, plus all test call sites in `core::flora`, `core::world`, `systems::growth`, and `systems::aging` (~30 occurrences, mostly indexed `vines[0]` test assertions). Added two new direct tests of the `FloraInstance` shape itself (`flora_instance_wraps_and_unwraps_a_vine_without_losing_identity`, `push_vine_adds_to_the_unified_organism_store_directly`) since prior coverage only exercised the enum indirectly through vine-specific helpers. `cargo check`/`clippy`/`fmt`/`test` all clean throughout (260/260 after this slice).
- `23:35 CEST` wired `core::greenhouse::GreenhouseState` into `WorldState` as inert data, closing the "`WorldState` attachment" gap the audit had flagged as still open: added `pub greenhouse: Option<GreenhouseState>` plus a `greenhouse_for_kind(kind)` match (mirroring the existing `scaffold_for_kind` pattern) that returns `None` for every current `WorldKind` (`Boot`/`MainScene`/`Sandbox`) — none of them are the greenhouse, so attaching real nursery data to them would be semantically wrong, unlike `ScaffoldState` which genuinely differs per kind. Deliberately did not add a `WorldKind::Greenhouse` variant, a render layer, or any reachable construction path that produces a populated greenhouse-attached world in the running app, per the roadmap's explicit "pure data, not runtime-ready" constraint. Added two tests: one confirming all three current world kinds construct with `greenhouse: None`, one confirming the `Option` slot genuinely accepts `GreenhouseState::nursery()` and round-trips correctly without disturbing flora/scaffold/kind state, proving the attachment point itself is sound. No other `WorldState` struct-literal construction sites exist outside `core/world.rs` (confirmed via repo-wide grep), so this was a fully contained change. 262/262 tests after this slice, `bash scripts/verify.sh` clean end to end.
- `23:50 CEST` updated `docs/greenhouse-roadmap.md`'s Current Baseline, 0.4 Readiness Snapshot, 0.4 Gate Checklist (`Flora storage decision` → `Locked`; `Greenhouse/world contract` → `Contract-ready, attachment wired, not runtime-ready`), Locked First-Pass Decisions, and Phase 4 execution-plan sections to reflect both changes above; left the dated `*_Ingest` historical brainstorming sections and `docs/vines.md`'s dated Phase 1 status untouched as historical record. Updated `docs/audit.md`'s unresolved-risks bullet and the stale "flora remains vine-shaped" risk note (now marked fixed, with the honest caveat that `run_growth` itself is still vine-specific since no second family exists yet to generalize it against). Updated `docs/architecture.md`'s "before the multi-family storage shape is chosen" line, since it now is. Updated `TODO.md`'s flora and greenhouse-prep sections to match. `bash scripts/check-docs.sh` and `bash scripts/verify.sh` both clean after the full doc pass.

## 2026-07-21 (continued) - Growth dispatch generalization

- `00:15 CEST` asked which piece of the remaining "Greenhouse/world contract" gate to tackle next and discussed the tradeoffs of three options (growth dispatch generalization, scoping `WorldKind::Greenhouse` itself, locking the species-profile data shape) plus a fourth "something else" — user picked growth dispatch generalization as the still-prep-flavored, lowest-risk-to-scope option; explicitly did not start on `WorldKind::Greenhouse` since that crosses from prep into a new selectable world/feature, which `AGENTS.md` says needs an explicit ask rather than autonomous judgment.
- `00:30 CEST` fixed a real generalization gap in `systems::growth::run_growth`: it found and grew only the single vine matching the hard-coded `BORDER_VINE_SEED_ID`, silently ignoring any other vine instance, unlike `systems::aging::run_aging` which already iterated every vine correctly. Refactored `run_growth` to loop over `world.flora.vines_mut()` (removing the `BORDER_VINE_SEED_ID` import entirely) and extracted the unchanged growth-rule body into a `grow_vine(&mut VineInstance)` helper. Added a regression test (`growth_advances_every_vine_not_only_the_first_seeded_one`) seeding a second, differently-id'd vine alongside the border vine and asserting both grow; confirmed the test is meaningful by temporarily reverting to the old hard-coded-id lookup and reproducing the exact failure it was written to catch, then restoring the fix. Existing single-vine behavior is preserved bit-for-bit (all 4 prior growth tests pass unchanged). `bash scripts/verify.sh` clean end to end, 263/263 tests.
- `00:35 CEST` closed the matching `TODO.md` item ("move the current border-vine growth rule away from one hard-coded seed branch") per the file's own rule that resolved items get removed with the closure recorded here, and updated `docs/audit.md`'s "Weakest Areas" #3 to record that growth/aging dispatch now iterates every vine instance, while being explicit that the growth *rule* itself is still vine-specific code pending a second family to generalize against.

## 2026-07-21 (continued) - Species-profile data-shape lock-in

- `00:50 CEST` asked again which piece to tackle next now that all prep-flavored work behind the greenhouse gate was exhausted (flora storage, `WorldState` attachment, and growth dispatch all closed); options were scoping `WorldKind::Greenhouse` itself (real feature work, still deliberately not started without an explicit ask) versus locking the species-profile data shape (small, independent, doesn't unblock the world model) versus something else — user picked the species-profile lock-in.
- `01:05 CEST` locked `docs/greenhouse-roadmap.md`'s remaining Open Decisions item ("should species definitions remain static Rust fixtures or move to structured files?") as static Rust fixtures, matching the roadmap's own existing bias and the pattern `core::flora::border_vine_species_profile()` already used in practice. Rather than treat "the fixture already exists" as sufficient proof, found the actual gap: `core::organism`'s only `SpeciesRegistry` test registered exactly one profile, never proving the registry (or the fixture-function pattern generally) actually holds more than one distinct profile at once. Added `species_registry_holds_multiple_distinct_profiles_independently`, registering two fixture-built profiles and asserting both resolve independently by id, plus that an unregistered id still returns `None`. No production code changed — `SpeciesRegistry`'s `Vec<SpeciesProfile>` already supported this; only the missing test coverage was the actual gap. `cargo fmt`/`clippy`/`test` clean, 264/264 tests.
- `01:10 CEST` moved the now-locked decision out of `docs/greenhouse-roadmap.md`'s Open Decisions list into Locked First-Pass Decisions (with the new test cited as evidence), updated the Phase 4 status/task list to mark both of its decision tasks done (only the "name actual candidate profiles" task remains, still gated behind a later work order), and removed the matching now-resolved `TODO.md` `inspect` item per the file's own closure rule. `bash scripts/check-docs.sh` and `bash scripts/verify.sh` clean after the doc pass.

## 2026-07-21 (continued) - WorldKind::Greenhouse: the first real feature-scope pass

- `01:20 CEST` asked to discuss `WorldKind::Greenhouse` — the one remaining item this whole session had deliberately stopped short of, since it crosses from prep into real feature work `AGENTS.md` says needs an explicit ask. Proposed the narrowest possible first slice (non-selectable `WorldKind::Greenhouse`, real profile + data attachment, zero visible change, mirroring how `Boot` already exists) and asked which scope the user actually wanted before touching anything. User chose the largest of the three offered options: selectable, with a minimal render layer.
- `01:35 CEST` before implementing, discussed the two remaining open sub-decisions in the open, grounded in what the roadmap already locks rather than re-litigating settled points: (1) selection UX — found the existing `w` hotkey (`cycle_world_kind()`) and a parallel persisted `WorldKindSnapshot` enum that mirrors `WorldKind` for save/load, proposed extending the same 2-world cycle to 3 rather than inventing a new entry point; (2) render treatment — cited the roadmap's own "the first visual artifact stays a docs/plain-text room sketch until pure data ownership is tested" constraint as reason to keep the render deliberately smaller than a finished room sketch (bounds outline + fixture markers, no labels, no per-fixture art), matching the existing `ScaffoldLayer`/world-border-debug-overlay precedent for simple line-based world geometry. User confirmed both before implementation started.
- `01:50 CEST` researched two subtleties before writing any code, to avoid silently getting them wrong: confirmed `WorldProfile.camera`/`grid` (`WorldCameraDefaults`/`WorldGridSpec`) are currently *inert, unwired* data — nothing in the runtime actually resets `ui_state.camera` on world switch, so reusing `DEFAULT_WORLD_GRID`/`DEFAULT_WORLD_CAMERA` unchanged was the correct, lowest-risk choice rather than inventing new camera-centering logic that would need to touch shared switch-handling code; and confirmed `WorldComposition` has no consumers outside `core::world.rs` besides a boolean `has_main_scene_composition()` check, so a new `WorldComposition::Greenhouse` variant (rather than reusing `SparseSandbox`) was free to add without any ripple effect, matching the existing one-variant-per-kind pattern.
- `02:10 CEST` implemented `WorldKind::Greenhouse`: added the enum variant, a new `WorldComposition::Greenhouse` variant, a `WorldProfile` entry (title/loading label, `selectable: true`, reused grid/camera defaults, `Empty` guide/population plans, capabilities all `false` except `pointer_probe`), added it to `WorldKind::SELECTABLE` (now 3 entries), extended the existing `greenhouse_for_kind`/`scaffold_for_kind` matches with real arms instead of wildcards, and added `WorldState::for_greenhouse()` alongside the existing `for_boot`/`for_main_scene`/`for_sandbox`. Then let the compiler's own exhaustiveness checking find every other site needing a `Greenhouse` arm rather than hunting manually — it surfaced 8 sites across `settings_layer.rs` and `ui/state.rs` (feature-tab content, `WorldKindSnapshot`, hero/companion/scaffold visibility gates, feature-settings item counts), each given the same "nothing in this world" treatment already used for `Boot`. Fixed one pre-existing test that assumed a 2-world cycle. All 265 tests green, clippy/fmt clean.
- `02:30 CEST` added the render layer: `scene/layers/greenhouse_layer.rs`, modeled directly on `ScaffoldRearLayer` (same `Layer` trait shape, same `SpatialResolver`-based projection helper, same `merge_grid` composition). Draws the active room's `GreenhouseBounds` as a single closed-path rectangle outline (`stroke_path` with 5 points, last repeating the first, `StrokeWeight::Hairline`, the existing but previously-unused `theme::style::accent_border()`) plus a marker (`theme::glyphs::GREENHOUSE_FIXTURE_MARKER = 'o'`, a new `theme::style::greenhouse_fixture_marker()` using the existing `palette::MARKER` color) at each fixture's anchor point. Only ever draws anything when `world.kind == WorldKind::Greenhouse` and a room resolves — `None` (nothing) otherwise, including for every other current world. Registered in `scene/layers/mod.rs` and `ui::scene::build_ui_layers()` at `z_index = 10` (the hero/entity band, since no hero exists in this world to collide with). Two new tests (draws bounds+marker in the greenhouse world; draws nothing in `MainScene`) plus a fix to the existing exact-z-index-list test. Clippy flagged one `let...else` that should have been `?` (the function already returns `Option`) — fixed. 267/267 tests, clippy/fmt clean.
- `02:45 CEST` did not stop at unit tests: built the release binary and drove it for real inside a detached `tmux` session (`tmux new-session`, `send-keys` for space/d/w/w, `capture-pane` to read the actual terminal output as text) — confirmed the boot sequence, dev-mode toggle, and world cycle all work end-to-end, that the greenhouse room genuinely renders (outline + 9 `o` fixture markers visible, matching the nursery's fixture count) with the correct "GREENHOUSE" / "World: greenhouse" labels, and that cycling once more correctly wraps back to a fully-intact main scene (hero and vines render as before — nothing regressed). This is the first change this session actually visually verified in the running app rather than only through `TestBackend`-based unit tests.
- `03:00 CEST` updated `docs/greenhouse-roadmap.md` (Current Baseline, 0.4 Gate Checklist — `Greenhouse/world contract` now "Selectable, minimal render live" — and Locked First-Pass Decisions, including closing the "first visual artifact stays a docs/plain-text sketch" constraint honestly: fulfilled, not abandoned, since the minimal render stayed deliberately smaller than a finished sketch), `docs/architecture.md`'s Active Layers list (`greenhouse room: z_index = 10`), `docs/audit.md` (Unresolved Risks, Weakest Areas #5, and superseding the same-day "attached but unreachable" note now that it's reachable), and `TODO.md`'s World Modes section. `bash scripts/check-docs.sh` and `bash scripts/verify.sh` both clean after the full pass.

## 2026-06-18 - Palette authority cleanup

- updated YAM's maintained theme docs so active shared palette authority now points at `~/_git/home/themes/palette/`
- moved packet-sheet references to `~/_git/legacy/archive-dotfiles/themes/` where they are archive inputs rather than nearby active source authority

## 2026-06-05

- `14:27 CEST` landed the first main-scene scaffold runtime slice after inspecting the repo's actual starting point: added `core::scaffold` as the world-owned home for a static rear hero-support cradle, attached that data to `WorldState` for the main scene only, and rendered it through a dedicated read-only `ScaffoldLayer` beneath the hero using the shared spatial/drawing path. The batch intentionally stops short of a foreground lip, decorative branch spread, or vine-led overgrowth so the support contract stays small and reviewable. `docs/main-scene-scaffold.md`, `docs/architecture.md`, `docs/scene-model.md`, `docs/rendering.md`, `TODO.md`, and `docs/audit.md` were updated to match.
- `14:49 CEST` turned sandbox into the preferred scaffold-prototyping room without creating a second composition pipeline: added persisted sandbox visibility policies for hero, companions, and scaffold in `UiState`, rewired the relevant render-layer guards through UI-owned visibility helpers instead of hardcoded main-scene checks, and widened the settings `features` / `gif` / `positions` tabs so sandbox trials can compare scaffold-only and fuller composition states. The docs now explicitly recommend sandbox-first scaffold review and keep masks deferred until a specific occlusion need appears.
- `17:05 CEST` fixed the last-mile sandbox toggle discoverability gap after review: opening settings in sandbox now lands directly on `features`, the help popup explicitly points prototype work to `settings > features`, and the debug panel now reports sandbox hero/companion visibility honestly instead of claiming they are main-scene-only.
- `17:33 CEST` repaired the broken scaffold visibility contract after screenshot review: main scene scaffold visibility is now a real persisted feature policy instead of being hardwired on, sandbox now reuses the same world-owned scaffold payload for prototype comparisons, and the settings `features` tab now switches its rows by active world so main-scene controls no longer masquerade as sandbox controls.
- `17:54 CEST` stopped treating the local scaffold brainstorming note as an implicit memory aid only: `docs/main-scene-scaffold.md` now distills the concrete coordinate anatomy from `/Users/mcq/Downloads/yam_tree_scaffold_brainstroming.md`, and `core::scaffold` was refit toward that authored graph with an explicit ground-trunk origin `(-20, -30)`, fork node `(-20, -24)`, rear back brace toward `(-67, 30)`, forward seat-ramp emergence toward `(50, -10)`, raised-leg branch toward `(25, 25)`, and a small foreground lip still kept narrow and reviewable.
- `14:56 CEST` landed the next scaffold recommendation as ordinary world geometry instead of masks: `core::scaffold` now includes a tiny foreground nesting edge alongside the rear support cradle, `scene/layers/scaffold_layer.rs` now renders rear and foreground scaffold strata through separate read-only layers around the hero, and the docs/audit now record that the current review question is whether this no-mask foreground lip is visually sufficient before any explicit mask seam is introduced.

## 2026-05-31

- `11:14 CEST` consolidated greenhouse planning back into a single authoritative `docs/greenhouse-roadmap.md` surface: the non-redundant operation stages, immediate next tasks, stop conditions, and handoff checklist from the temporary greenhouse TODO draft were folded into the roadmap, while `docs/README.md`, `TODO.md`, and `docs/audit.md` were updated to remove the second active greenhouse TODO pointer.
- `11:26 CEST` trimmed the root backlog's greenhouse section back to execution gates and a single roadmap pointer: detailed candidate identity, organism, fixture, visual-review, and phase-task material now lives only in `docs/greenhouse-roadmap.md`.
- `11:29 CEST` distilled duplicated flora and greenhouse prose out of the architecture, scene-model, and rendering contracts: architecture now keeps ownership boundaries, scene-model keeps behavior vocabulary, rendering keeps visualization rules, and detailed candidate species / fixture / environment planning points back to `docs/greenhouse-roadmap.md`, `docs/vines.md`, and `docs/glossary.md`.
- `11:34 CEST` clarified the documentation map by replacing the duplicated change-target list in `docs/README.md` with a compact role table for active owners, including greenhouse, vines, weather, hero cache, and config surfaces.
- `16:23 CEST` ingested the ChatGPT greenhouse preflight and first brainstorming notes into the active planning docs without promoting runtime work: `docs/greenhouse-roadmap.md` now records the 0.4 readiness gates, functional-space-first room ordering, bounded reference lanes, and current enum-backed flora-store bias, while `TODO.md`, `docs/audit.md`, and `docs/resource-map.md` were aligned around those constraints.
- `16:38 CEST` completed a non-feature 0.4 expansion readiness review: the baseline `scripts/verify.sh` run passed, `known_issues.md` remained empty, `cargo tree -d` reported no duplicated dependency graph, targeted world-profile / flora-adapter / Chafa fallback tests passed, and the roadmap plus audit now record that no new guard/test was needed because remaining 0.4 work is contract-decision work rather than an active regression.
- `16:43 CEST` completed a docs-only greenhouse functional-space contract pass: `docs/greenhouse-roadmap.md` now has a 0.4 gate checklist, locked first-pass decisions, and a pure data contract for future `core::greenhouse` state, rooms, access paths, zones, fixtures, planting sites, symbolic environment profiles, and read-only inspection records; `TODO.md` and `docs/audit.md` were aligned without adding runtime behavior.
- `17:08 CEST` copied the full desktop greenhouse brainstorming corpus into `docs/archive/greenhouse-brainstorming/` and ingested the not-yet-promoted ecosystem design notes plus roadmap review into `docs/greenhouse-roadmap.md`: the active roadmap now preserves bounded lab/frame vocabulary, shell/support/light/infrastructure families, identity hierarchy, and journal-target ordering while still keeping the first implementation slice constrained to one inert `greenhouse_nursery` room rather than greenhouse-global chrome or multi-lab runtime behavior.
- `17:28 CEST` added `docs/chatgpt-0.4-source-pack/` as a compact upload set for the separate YAM 0.4 ChatGPT project: the pack now provides a project brief, architecture constraints, readiness/gates summary, greenhouse brief, and ChatGPT usage guide so external brainstorming can stay aligned with current world/spatial/flora/greenhouse contracts instead of inferring them from the whole repo.
- `17:34 CEST` widened `scripts/check-docs.sh` to lint and spell-check the new `docs/chatgpt-0.4-source-pack/*.md` surface as active documentation, and aligned `docs/hygiene.md` with that broader docs-check scope so the external-upload pack stays under the same hygiene guard as the main contract docs.
- `17:43 CEST` added `docs/chatgpt-0.4-source-pack/source-pack-manifest.md` after reviewing external ChatGPT feedback: the upload pack now carries explicit provenance for source repo, branch, commit, owning docs, and refresh rules, while still remaining a derived planning context rather than a competing authority layer.
- `18:02 CEST` reworked the full `docs/chatgpt-0.4-source-pack/` set into a more coherent briefing bundle: the pack now has an explicit upload order, cleaner first-pass versus later-phase framing across the project/greenhouse/readiness briefs, a compact `repo-workflow-brief.md` for authority and workflow cues, and tighter prompting guidance so external ChatGPT sessions stay useful without pretending the pack is a full repo mirror.
- `18:10 CEST` applied a small external-feedback cleanup to the ChatGPT source pack: normalized the greenhouse first-pass ordering string to include `access paths` and the fuller `environment profile` / `inspection surface` wording, and fixed the manifest's included-files list so it matches the actual uploaded export set.
- `18:16 CEST` tightened the ChatGPT source pack again after another external review: `readiness-and-gates.md` now says the flora-storage direction is selected while the exact shape remains pending, and `source-pack-manifest.md` now spells out what the pack does not include, how it is exported, what `verification green` refers to, and the optional future checksum path for stronger drift detection.

## 2026-06-01

- `06:36 CEST` began the real `0.4` greenhouse implementation path with the first authorized inert core slice: added `src/core/greenhouse.rs` and exported it through `core/mod.rs`, defining stable string-like ids, one tiny `greenhouse_nursery` room, access paths, zones, fixtures, planting sites, a symbolic room-level environment profile, and read-only inspection references. The module stays pure data with invariant tests only; it is not yet attached to `WorldState`, rendering, UI, systems, or weather code. `TODO.md`, `docs/architecture.md`, `docs/audit.md`, and `docs/greenhouse-roadmap.md` were updated to reflect the landed state.

## 2026-06-02

- `10:34 CEST` studied `ntrospect0/glint` as a Rust/Ratatui contrast case and promoted the result into the repo-owned docs instead of leaving it in chat memory: `docs/resource-map.md` now records Glint as a dashboard-TUI reference whose useful lessons are infrastructure-only (registry, feature gating, polling/config/theme helpers, setup flow), while `docs/audit.md` now explicitly records dashboard drift as an active caution for future YAM UI and greenhouse work.

## 2026-06-04

- `10:34 CEST` ingested the desktop main-scene scaffold brainstorming note into the active docs: added `docs/main-scene-scaffold.md` as the owning hero-support scaffold direction note, linked it from `docs/README.md`, and aligned `docs/scene-model.md`, `docs/rendering.md`, `docs/vines.md`, and `TODO.md` so future scaffold ideation stays centered on a static world-attached seat/back cradle read before decorative branching or vine overgrowth.
- `10:52 CEST` cleaned up the front-door README after the direct GitHub edit that added the hero GIF preview: updated the preview note to describe the current GitHub-hosted media honestly, added the new `docs/main-scene-scaffold.md` owner link to the active surface map, and removed the duplicated current-release line from the version/history section.
- `10:58 CEST` tightened `scripts/check-docs.sh` after the README GIF change exposed an over-strict assumption: the front-door asset check still fails on missing local asset paths, but now skips explicit `http` / `https` media URLs so a deliberate GitHub-hosted preview image does not trip the local-path hygiene guard.
- `13:53 CEST` repaired the front-door README after a broader rewrite softened too many project guardrails: kept the GitHub-hosted GIF and a short personal note, but restored the stronger scene-engine framing, explicit anti-dashboard posture, launcher-behavior detail, broader docs map, scaffold-owner link, and the more stable contract-first front-door structure.

## 2026-05-30

- `08:01 CEST` extended the ownership-boundary guard from core into systems during the spatial migration: `src/systems/growth.rs` now uses `core::spatial::SpatialPoint` directly instead of the `scene::coords::WorldPos` compatibility alias, and `scripts/check.sh` now fails if systems import scene, render, UI, or terminal modules. `AGENTS.md`, architecture, hygiene, audit, and backlog docs were updated so the executable check matches the documented systems contract.
- `08:09 CEST` continued render/spatial tidying without changing visible guide behavior: `render::guide` now projects guide points through `core::spatial::SpatialResolver` instead of the scene compatibility helper, keeps one resolver per guide draw batch, and uses checked signed-to-grid coordinate conversion for guide cells so off-screen guide geometry cannot rely on lossy casts. The render guide tests now include an off-screen no-wrap case, and the architecture, scene-model, rendering, audit, and backlog docs were updated.
- `08:18 CEST` continued render/spatial hardening on the vine path: `scene::layers::vine_layer` now projects stems and organs through a per-render `core::spatial::SpatialResolver` instead of rebuilding projection through the scene compatibility helper, and `render::drawing` now uses checked signed-to-grid conversion for both glyph stamps and stroked path cells so oversized coordinates cannot wrap into the frame. Drawing tests cover the no-wrap case, and the vine/rendering/architecture/audit/backlog docs were updated.
- `08:28 CEST` moved the remaining `RenderState` companion projection helpers off the scene compatibility projection function: clock, weather, date, and calendar screen positions now use a local `core::spatial::SpatialResolver`, while tests assert against the same core resolver instead of `scene::coords::project_world_to_screen(...)`. Architecture, rendering, scene-model, audit, and backlog docs were updated to distinguish core-spatial render paths from remaining compatibility projection paths.
- `08:41 CEST` moved active hero rendering onto `core::spatial::SpatialResolver` as well: `scene::layers::hero_layer` now projects hero rows without the scene compatibility helper and uses checked signed-to-grid row placement so oversized positive screen X values cannot wrap back into the frame. Hero-layer tests cover negative clipping and oversized-X no-wrap behavior, and the spatial ownership docs were updated so only debug and compatibility element projection remain on the scene facade.
- `08:47 CEST` moved the debug projection bundle onto `core::spatial::SpatialResolver`: `scene::layers::debug_layer` now projects hero telemetry, pointer probes, soft-band probes, world frame, world axis, and datum markers without calling the scene compatibility projection helper, and debug marker writes now use checked signed-to-grid conversion. The active render-state projection test in `scene/mod.rs` now asserts against the core resolver too, leaving `project_world_to_screen(...)` isolated inside the scene compatibility module and its tests.
- `09:02 CEST` tightened the spatial compatibility boundary after the projection migration: the active clock/date/weather layers and `ui::anchor` now import signed screen positions from `core::spatial` instead of the `scene::coords::ScreenPos` compatibility alias, and `scripts/check.sh` now fails if `project_world_to_screen(...)` appears outside `src/scene/coords.rs`. Hygiene, architecture, rendering, scene-model, audit, and backlog docs were updated with the executable guard.
- `14:05 CEST` moved `UiState::hero_scene_attachment()` off the `scene::coords::WorldPos` compatibility alias: UI-owned hero/companion offsets now build the scene attachment object with `core::spatial::SpatialPoint` directly while preserving the existing `scene::entity::HeroSceneAttachment` boundary. Focused attachment tests stayed green, and architecture/audit docs were updated to record the cleaner UI-to-spatial handoff.
- `18:29 CEST` finished isolating the `scene::coords` compatibility facade from the rest of `src`: remaining layer test fixtures now import `core::spatial::SpatialPoint` directly, and `scripts/check.sh` fails if `crate::scene::coords` appears outside `src/scene/coords.rs`. The spatial backlog, hygiene, architecture, rendering, scene-model, and audit docs were updated to record the stronger guard.
- `18:40 CEST` added the preliminary greenhouse expansion roadmap as a dedicated planning contract before creative input or runtime greenhouse work starts. `docs/greenhouse-roadmap.md` now owns the north star, hard rules, domain model, phased gates, first implementation sequence, open decisions, and a bounded external creative-input brief; README, docs index, backlog, and audit notes were updated to point at that owner instead of scattering greenhouse planning across chat memory.
- `19:02 CEST` cleaned up `docs/greenhouse-roadmap.md` as a direct ChatGPT brainstorming reference by adding a self-contained reference section, idea-shape constraints, small-catalog prompt patterns, and follow-up prompts for room, species, fixture, lifecycle, and sequencing passes.
- `19:38 CEST` ingested the first greenhouse brainstorming session as candidate planning material only: the roadmap now records the nursery/propagation-room starting bias, first organism candidates, static room sketch, zone/fixture vocabulary, label and interaction sketches, symbolic environment model, curation/progression language, transfer gates, and candidate fixture states while TODO and audit notes keep those ideas gated from runtime implementation.

## 2026-05-29

- `14:12 CEST` continued the pre-expansion architecture prep batch without adding greenhouse runtime behavior: `WorldKind::profile()` now owns grid, camera defaults, guide plans, population plans, and capabilities for the current boot/main-scene/sandbox worlds; `core::organism` now includes an in-memory `SpeciesRegistry` and per-instance `OrganismJournal` skeleton; `FloraState` exposes family-count and organism-identity adapters; and `scene::coords` now routes entity-backed anchor lookup through `core::spatial::SpatialAnchorLookup`. The active architecture, scene-model, rendering, vines, audit, and backlog docs were updated with the new seams.
- `14:19 CEST` added the root `AGENTS.md` operating guide as the first agent-facing workflow surface for YAM: it points agents at the canonical docs, records strict worktree/architecture/verification rules, and keeps future skill guidance procedural instead of duplicating contracts. `scripts/check-docs.sh`, README, docs index, hygiene notes, and the backlog now treat it as an active root doc.
- `14:19 CEST` added the first repo-local skill drafts, `skills/yam-maintenance/SKILL.md` and `skills/yam-architecture-review/SKILL.md`, as concise workflow triggers for repeated maintenance and architecture-review work. The docs checker now includes `skills/*/SKILL.md`, and the README, docs index, hygiene rules, and backlog identify those skills as procedural helpers rather than alternate contract sources.
- `14:19 CEST` completed the first skill-metadata hygiene slice by generating `agents/openai.yaml` UI metadata for the two repo-local skills and teaching `scripts/check-docs.sh` to require matching skill names, metadata files, valid short descriptions, and default prompts that explicitly mention the skill. `AGENTS.md`, hygiene notes, and the backlog now record that repo-local skills need matching metadata.
- `14:31 CEST` tightened repo-local skill hygiene validation in `scripts/check-docs.sh`: skill folders now require lowercase hyphen-case names matching directory names, frontmatter descriptions, `agents/openai.yaml` interface metadata, 25-64 character short descriptions, and default prompts that explicitly mention the skill. `AGENTS.md`, hygiene notes, and the backlog were updated to reflect the stronger skill contract.
- `14:37 CEST` continued spatial-relation consolidation without changing visible runtime behavior: `core::spatial::SpatialScreenPoint` is now the signed screen-space type used for on-screen and off-screen projections, `scene::coords::ScreenPos` is a compatibility re-export, and simple scene attachment composition now resolves anchor/offset math through `core::spatial` directly. Architecture, scene-model, rendering, audit, and backlog docs were updated to record the tighter boundary.
- `14:42 CEST` hardened the core/scene ownership boundary during the spatial migration: `src/core/flora.rs`, `src/core/world.rs`, and `render::render_state` now use `core::spatial` point types directly instead of depending on `scene::coords` aliases, and `scripts/check.sh` now fails if `src/core` imports `crate::scene::`. Architecture, hygiene, audit, and backlog docs were updated with the new guard.

## 2026-05-28

- `06:01` continued the pre-expansion architecture batch by adding `core::organism` as the first shared flora identity vocabulary: organism ids, species ids, journal ids, lifecycle state, generic stats, identity grouping, organism family, and a species-profile payload shape now exist before any second plant family or greenhouse population is introduced. The current border vine now uses those shared id/stat/lifecycle types and exposes a border-vine species profile while keeping visible runtime behavior unchanged.
- `06:23` continued the general architecture pass by moving switchable-world ordering and transition labels into `WorldKind`: `WorldKind::SELECTABLE` now owns the current main-scene/sandbox cycle, `Boot` is explicitly non-selectable, and UI persisted snapshots convert through the core world contract instead of carrying a binary world toggle as hidden UI policy. The docs now keep greenhouse/lab worlds pointed at that seam without introducing them yet.
- `12:44` continued preparatory world architecture by adding `WorldKind::profile()` and coarse `WorldComposition` metadata for the current boot, main-scene, and sandbox worlds. Main-scene-only companion/render behavior now branches through the shared composition predicate instead of repeating direct world-kind checks, leaving future greenhouse/lab worlds pointed at an explicit profile seam before any population/storage mechanics are introduced.
- `16:19` continued the spatial-prep pass by making the central companion projection path screen-typed: `project_world_to_screen(...)` now returns signed `ScreenPos`, `RenderState` companion screen helpers use that type, and clock/weather/date/debug consumers no longer receive world-position aliases for those projected coordinates. The older `world_to_screen(...) -> WorldPos` helper remains as an explicit compatibility wrapper for guide, vine, and raw debug line paths that still need a later migration.
- `16:24` narrowed that spatial compatibility wrapper further: hero rendering, guide drawing, and vine stems/organs now use `project_world_to_screen(...)` directly, leaving the legacy `world_to_screen(...) -> WorldPos` path concentrated around the debug projection bundle and older bridge tests rather than ordinary world-prop rendering.
- `16:30` finished the current typed projection cleanup by moving the debug projection bundle to `project_world_to_screen(...)` as well and deleting the scene-level `world_to_screen(...) -> WorldPos` wrapper. Active render projections now return `ScreenPos`; the remaining world-shaped compatibility debt is the explicit `resolve_position(...)` bridge for older call sites.
- `16:41` removed the last world-shaped screen bridge from `scene::coords`: compatibility element projection now returns `ScreenPos` through `resolve_element_screen_position(...)`, and the older `resolve_position(...) -> WorldPos` helper is gone. The remaining spatial-prep focus is now entity-backed anchoring ownership rather than screen/world type leakage.

## 2026-05-24

- `12:23` recorded the Apple Music companion feasibility assessment in `docs/apple-music-companion.md` and linked it from the docs map/resource map: the future-friendly path is a separate Rust/Ratatui `yam-music` companion that delegates playback to macOS Music.app first, while Chrome/CDP/MusicKit playback and pure Rust Apple Music streaming stay explicitly risky or rejected first paths.
- `12:38` refreshed the Cargo lockfile to the latest semver-compatible dependency set and removed the stale direct `portable-pty` dependency after confirming no runtime, script, docs, or wrapper path referenced it anymore; this pruned the old PTY/serial transitive graph while keeping YAM on the stable Rust toolchain.

## 2026-05-19

- `06:56` activated the full docs-hygiene toolchain on this workstation by installing `markdownlint-cli`, `markdownlint-cli2`, and `cspell` through the tracked dotfiles/Homebrew path, then tightened YAM's shared cspell dictionary for the stable current vocabulary and legacy hotkey-label fragments that the now-live spellcheck pass surfaced in active docs.
- `06:19` hardened runtime/process hygiene in one maintenance pass: `main.rs` now propagates command failures for helper paths like `--update` and `--check-updates` instead of silently treating non-zero exits as success, and `runtime.rs` now keeps alternate-screen/raw-mode teardown in a small drop guard so later event-loop errors are less likely to leave the terminal in a broken state.
- `06:19` tightened front-door/docs hygiene to catch a real drift case instead of only fixing the visible symptom: removed the broken README preview placeholders that pointed at missing `docs/assets/...` files, taught `scripts/check-docs.sh` to fail on missing local asset references from `README.md`, updated `docs/hygiene.md` to make that rule explicit, and refreshed the audit snapshot to record the reduced risk.
- `10:40` opened the pre-expansion architecture batch before main-scene enrichment or greenhouse ecosystem work: rewrote `TODO.md` into an honest preparation queue, aligned `docs/vines.md` with the live Phase 1-7 vine runtime, added architecture/scene-model gates for shared organism identity and multi-world selection, expanded docs checks to the first-level active docs surface, hardened the hero live-compiler path to fail soft without poisoning the hero cache, replaced stale density/constraint phase TODO stubs with explicit inert contracts, and aligned direct `crossterm` with Ratatui's current dependency version.

## 2026-05-15

- `00:33` bumped the crate and front-door release marker from `0.3.8` to `0.3.9` after the quit-transition cleanup pass: this release carries the live-scene linear dissolve on accepted quits and the launcher handoff polish that returns to an in-terminal fastfetch refresh instead of a separate window or a stale shell trace.
- `00:16` turned clean live quits into a real runtime transition instead of an immediate alternate-screen snap: when `[q]` is accepted in the live scene, YAM now runs a seeded `tachyonfx` dissolve for `1s` with linear interpolation before yielding back to the shell, and the same dissolve now also follows the dirty-state `save and quit` / `discard and quit` confirmation paths.

## 2026-05-14

- `19:41` tightened the post-exit fastfetch refresh to mirror the real Ghostty startup framing more closely: after the wrapper clears the screen, it now surrounds `fastfetch-chafa` (or the config-based fallback) with the same blank-line spacing the shell startup hook uses, which should reduce the last visible top-of-window mismatch between a true fresh terminal and the YAM-quit refresh path.
- `19:35` corrected the post-exit terminal refresh to use the same fastfetch entrypoint as the real fresh Ghostty shell startup instead of bare `fastfetch`: the YAM wrappers now prefer `~/.local/bin/fastfetch-chafa`, which in turn uses the custom dotfiles config plus the generated image/logo path, and only fall back to `fastfetch --config $HOME/_git/dotfiles/fastfetch/config.jsonc` if that richer wrapper is unavailable.
- `20:04` retired the last known legacy `dotfiles` path dependency from the YAM launchers. The fallback post-exit refresh in `bin/yam` and `bin/yam-sandbox` now points at the managed live Fastfetch config under `~/.config/fastfetch/config.jsonc` instead of `~/_git/dotfiles/fastfetch/config.jsonc`, which keeps the fresh-shell visual reset working without requiring the old repo to stay in the active `_git/` workspace.
- `19:26` extended the in-terminal launcher refresh so quitting YAM can return to something much closer to a fresh shell window: after the runtime exits, the `yam` and `yam-sandbox` wrappers now clear the screen/scrollback again and invoke `fastfetch` when available before handing control back to the prompt. The behavior can be disabled per-run with `YAM_POST_FASTFETCH=0`.
- `19:19` tightened the in-terminal launcher behavior so quitting YAM now returns to a visually fresh shell instead of leaving the old install/output trace on screen: the `yam` and `yam-sandbox` wrappers now clear both the visible screen and scrollback immediately before launching the runtime in the current terminal.
- `19:12` reverted the launcher wrappers back to in-terminal execution after the separate-window behavior proved disruptive: `yam` and `yam-sandbox` no longer open Ghostty as a new window and now return cleanly to the current shell after quit, while still keeping the self-aware installed-runtime refresh path intact.
- `18:56` removed the last loading-screen footer hint remnants after the bar/layout cleanup: boot loading now keeps the footer row visually empty all the way through the wait-for-space phase and dissolve, while the existing `[q]uit` / `[d]ev` key handling remains live in the input layer instead of being advertised inline.
- `18:39` dropped the attempt to reuse Oh My Posh's Powerline seam mechanics directly in the transparent loading bar after screenshot review made the failure mode obvious: in this terminal/font stack those glyphs behave like dense background-transition blocks, not delicate foreground progress marks, so the bar collapsed into a visual blob. The loading bar now keeps the OMP-derived palette but switches to a thinner transparent grammar: `━` fill, `╸` active edge, and `┄` empty tail.
- `18:07` tightened the new OMP-inspired loading line after live review surfaced two real behavior bugs: the `loading` label now reserves a fixed three-cell dot suffix so the line no longer re-centers itself while animating, and the progress bar now uses the proper Powerline role sequence instead of one reused body glyph family: `e0b6` left cap, `e0c4` filled body, `e0c6` filled seam, `e0c7` empty seam, `e0c5` empty body, and `e0b4` right cap.
- `17:33` restyled the active loading label/bar around the real Oh My Posh vocabulary already in use on this machine instead of copied chat glyphs: the loading label now keeps calm slate text with a greener animated dot suffix, and the bar now uses an OMP-inspired Powerline family (`e0b6` / `e0b4` caps plus `e0c4` / `e0c5` body glyphs) with darker-green fill, brighter-green live edge, and slate empty tail so boot progress echoes the prompt language without competing with the green FIGlet wordmark or the blue ready prompt.
- `17:16` finally fixed the loading-screen caption/logo collision at the real source instead of through more row nudges: the FIGlet `yam` loader now trims leading fully blank font rows before layout, and its render path uses a dedicated compositor writer that skips spaces entirely so an invisible top FIGlet row can no longer repaint `a very special` green. This keeps the visible logo tucked directly under the caption without reviving the old style-bleed bug.
- `12:47` identified the real loading-screen color-bleed cause after several misleading row nudges: the FIGlet logo path was still using the general text writer, which applies foreground color even on spaces, so any shared caption/logo row could repaint `a very special` green without changing its glyphs. The loading logo now renders through the ASCII writer path that preserves the existing no-restyle-on-space contract, and the explicit row layout again keeps the caption and logo on separate rows.
- `12:39` stepped back from the repeated loading-screen row nudges and replaced the old shared-offset layout with explicit named rows for the caption, FIGlet wordmark, version/build line, and lower prompt/bar area; this isolates the subtitle from the logo block so vertical tuning stops causing the green wordmark style to bleed into `a very special`.
- `12:24` backed the main-scene footer away from the earlier help/version catling after screenshot review: the right side now stays visually quiet with only the version/build stamp on the main scene, while the left-side `[q]uit • [d]ev` anchor remains intact and the denser help affordance stays out of that frame.
- `12:18` refined the footer into a clearer split catling: the always-on left hint is back to the stable `[q]uit • [d]ev` anchor, while `[?] help` moved to the right and now sits beside the version/build stamp instead of sharing the left-side command cluster or competing with dev-only move vocabulary.
- `12:11` tightened the footer/help contract without changing the broader surface family: the normal footer now keeps global help visible alongside quit/dev entry, the dev footer swaps in `[m]ove` instead of repeating the same pre-dev hint, and the help popup stopped wasting a body row on “open help” once it is already open so the denser dev-only vocabulary has clearer ownership there.
- `12:03` tightened the debug info panel without changing the broader dev overlay surface: the `runtime` tab now stays focused on session/control plus camera/pointer facts, `hero` carries the hero-specific animation and placement readout, and the soft-band probe moved to the `vines` tab with the rest of the spatial/guide inspection facts so the panel reads less like a mixed dump and more like four deliberate inspection groups.
- `11:57` added `yam-diagnostics` as the first small reader for the new local NDJSON telemetry: it ships alongside the other launcher wrappers, summarizes recent sessions by default, can tail raw events or focus one session id, and keeps diagnostics convenient without promoting a larger analytics or always-on telemetry system.
- `11:50` validated the new opt-in diagnostics path with a real local smoke test: `YAM_DIAGNOSTICS=1` produced compact NDJSON install events with a shared session id, and the first observed traces immediately showed brief Cargo package-cache lock waits during install/verify flows, which is a stronger explanation for the recent `yam-install` timing wobble than any YAM runtime startup regression.
- `11:28` added the first tiny opt-in local diagnostics path for install/runtime timing: `YAM_DIAGNOSTICS=1` now writes NDJSON events to the user state dir, the launch wrappers mint/share a session id when diagnostics are enabled, `scripts/update.sh` records install-step timing plus offline-first vs network-fallback mode, and `runtime.rs` records boot start, boot-phase transitions, and the first world-ready milestone without turning on any always-on telemetry.
- `11:28` fixed a real launcher regression behind the Ghostty “Configuration Errors” dialog: the `yam` wrappers were forwarding `/bin/zsh -lc ...` through `open -na Ghostty --args` without an exec flag, so Ghostty interpreted those tokens as config override fields (`/bin/zsh`, `-lc`, and the shell command all showed up as `invalid field`). The macOS launch path now uses `-e /bin/zsh -lc ...` explicitly so Ghostty treats the shell command as the program to run instead of as config syntax.
- `11:28` cleaned up the launcher path so `yam` and `yam-sandbox` are finally self-aware instead of always dragging Cargo into normal launches whenever the repo checkout exists: both wrappers now prefer the installed runtime, refresh through the current offline-first update path only when the installed binary is missing or older than repo runtime inputs, and keep `YAM_USE_REPO_RUN=1` as an explicit escape hatch for intentional direct `cargo run --release` development sessions.
- `11:28` fixed the repo-side install-path ambiguity behind the recent timing confusion: added a real `bin/yam-install` wrapper, taught `scripts/update.sh` to install it alongside `yam` and `yam-sandbox`, and aligned the front-door/audit wording so the documented `yam-install` command now actually routes through the current offline-first update path instead of depending on some older external shell alias or wrapper.
- `11:28` closed the loading-screen centering pass after screenshot confirmation: the full loading assembly now reads visually centered again with the explicit one-cell prompt nudge, so `known_issues.md` remains empty for the moment instead of carrying `KI-001` forward.
- `11:28` inspected the pasted `yam-install && yam` timing history and recorded the honest interpretation in the audit: the observed `1.64s` to `21.78s` spread is currently better explained by Cargo/install-path variance than by YAM runtime startup, and the output shape looked like the older direct Cargo install path rather than the newer offline-first `scripts/update.sh` wrapper because it showed repeated `Updating crates.io index` lines and none of the wrapper's `[yam] ... (offline-first)...` markers.
- `11:16` applied one final screenshot-led visual-centering adjustment to the wait-for-space prompt: after restoring true terminal-center alignment, the prompt still read slightly left by eye, so it now uses an explicit one-cell right nudge as the intended visual-center rule and the focused loading-layer regression test was updated to pin that exact presentation choice.
- `11:16` corrected the premature `KI-001` closure after screenshot review showed the wait-for-space prompt was still visibly right-shifted in the terminal: the prompt now centers on the terminal window itself instead of sharing the version/build row's offset centerline, and the focused loading-layer regression test now asserts terminal-center alignment rather than version-line alignment.
- `11:16` closed `KI-001` with one deliberate loading-layout pass instead of another blind nudge: the wait-for-space prompt no longer reuses the active-loading label's old `+1` centerline offset, now shares the version/build row centerline directly, and is covered by a focused regression test that checks the rendered prompt center against the rendered version-line center before the issue was removed from the active register and backlog.
- `11:16` extended `scripts/check-docs.sh` with one more high-value active-doc invariant: it now verifies that the canonical current-release line in the root `README.md` matches `Cargo.toml`, so version bumps have one explicit package/front-door consistency guard instead of relying on memory during release prep.
- `11:14` added `scripts/verify.sh` as the canonical repo-owned full verification gate for maintenance and release work: it now composes `scripts/check-docs.sh`, `scripts/check.sh`, and the full `cargo test --quiet` suite in one command, while the lighter docs-only and Rust-only checks remain available for narrower slices.
- `10:44` added `scripts/check-docs.sh` as the first repo-owned docs-hygiene command for active maintenance batches: it now verifies the active-doc inventory and `TODO.md` -> `known_issues.md` issue-id linkage directly, and it opportunistically runs the repo-configured `markdownlint` / `cspell` checks when those CLIs are installed locally, so docs work has one canonical verification entrypoint without forcing a new Node toolchain contract on the repo.
- `10:44` completed the first post-`0.3.8` dev-UI contract-tightening batch without changing runtime behavior: kept backward-compatible persisted-state decoding while clarifying the active settings vocabulary around `ui`, aligned the active docs/backlog/audit away from stale `widgets` wording, updated the scene/render/architecture contracts to describe the current long-lived `Scene` runtime path instead of the old temporary-scene wording, and tightened the active debug-language posture so the docs match the current dev-gated diagnostic surfaces.
- `10:32` bumped the crate and runtime release marker from `0.3.7` to `0.3.8` after the release-readiness pass: this release carries the render-loop optimization wave, the repo simplification onto `main`, the front-door/docs-role cleanup, the shared markdown/spelling hygiene configuration, and the explicit pre-bump honesty posture that keeps `KI-001` documented as the one minor non-blocking known issue.

## 2026-05-13

- `02:46` ran a final pre-bump coherence audit across the repo front door, docs map, backlog, issue register, and audit snapshot; the set is now aligned on one explicit release-honesty point: `KI-001` remains the only tracked known issue, and it is currently treated as a minor non-blocking boot-screen centering issue rather than something to “fix” blindly without a deliberate visual pass.
- `02:27` removed the temporary file-local `cspell:ignore` comments from the active docs that were covered by the new shared repo dictionary, and wired the workspace editor settings toward the shared spell/lint configuration so local VS Code behavior tracks the repo-level hygiene contract more closely.
- `02:18` added shared repo-level `.cspell.json` and `.markdownlint.jsonc` configuration after the markdown/spell cleanup pass, and updated `docs/hygiene.md` so active docs now have one explicit spell/lint contract: keep real markdown errors and true typos fixed, keep recurring valid repo vocabulary in shared config, and avoid pseudo-link bracket tags like `[fix]` or `[12:34]` in favor of code-style labels.
- `01:55` ran the narrow post-rename wording sweep after moving the repo to `main`: there were no meaningful live branch-policy stragglers left outside history/archive docs, but the front door still opened with `yam-rust`, which now read more like a retired branch name than a repo identity. The root README now presents the project as `yam`, while the glossary explicitly keeps `yam-rust` defined as the current crate/binary name instead of an active branch label.
- `01:42` completed the branch-model simplification on GitHub and locally: archived the last `yam-go` tip under the `archive/yam-go-final` tag, renamed the active maintenance line from `yam-rust` to standard `main`, switched the GitHub default branch to `main`, and deleted the legacy `yam-go` and `yam-rust` remote branches so the repo now has one active development branch again.
- `01:31` simplified the repo branch model to match the real current project shape: prepared the active branch-policy docs for a move from the legacy `yam-rust` branch name to standard `main`, with the old `yam-go` line treated as archival history rather than a second active development branch.
- `01:12` ran the first interactive profiling-oriented runtime sample after the recent optimization wave and got a useful negative result: direct `--version` timing still says the binary startup path is cheap, but an automated “launch the real TUI and quit” wall-clock sample came in around `33.89s`, which is clearly dominated by the intentional boot/loading sequence and tty orchestration rather than ordinary frame-loop cost. The audit/backlog were tightened accordingly: future profiling should sample inside the live loop or through a boot-bypass harness instead of treating boot wall time as a render benchmark.
- `00:58` expanded `docs/glossary.md` into a broader shared vocabulary file instead of a narrow plant-only glossary: it now groups YAM-specific scene/UI/render terms, Rust/runtime terminology, docs-maintenance jargon, and the existing flora/morphology language so recurring repo terms like `move strip`, `scratch grid`, `cache-first`, `compatibility path`, `front door`, and `append-only log` have one canonical definition surface.
- `00:43` ran the first systematic markdown-role cleanup pass across the repo front door and issue/work-order surfaces: rewrote the root `README.md` to stay a lighter runtime/repo overview, rewrote `docs/README.md` into a clearer grouped docs map, tightened `known_issues.md` into a small id-based active-issue register, and added the first explicit backlog-to-issue link (`KI-001`) in `TODO.md` so issue tracking and execution steps stop drifting apart.
- `17:15` recorded the next runtime/code efficiency track before touching the hot paths: the active audit/backlog now treats live hero-frame compilation through GIF decode plus per-frame `chafa` startup work as the first optimization target, followed by persistent `Scene`/layer reuse, reusable full-frame `Grid` buffers, and only then smaller compositor/runtime-branch cleanups. This keeps the next performance work ordered and conservative instead of mixing it into unrelated UI polish.
- `17:20` made the first hero-cache optimization slice concrete in the owning docs: the render/architecture notes now explicitly describe the intended `HeroFrameSet` runtime path, the narrow migration order for moving Chafa toward an offline/cache-builder role, and the acceptance bar for replacing the common-path GIF decode + temp-file + process-spawn startup cost without changing the live hero baseline.
- `17:29` landed the first code-side hero-cache seam without changing startup behavior: added `render/hero_cache.rs` with a serializable `HeroFrameSet` / `CellGrid` / `CachedCell` / `CachedStyle` shape, round-trip tests for line/style preservation plus disk serialization, and a small owning note in `docs/hero-cache.md` so the next optimization step can add a runtime loader without inventing a second frame format on the fly.
- `17:42` wired the first cache-first hero startup path into the live runtime: `Hero::new()` now prefers a prepared `HeroFrameSet` cache and only falls back to the old GIF-decode + temp-frame + `chafa` compile path when the cache is missing, stale, or invalid, which keeps the current Chafa visual baseline while finally moving the common startup path off unconditional per-frame shelling-out work.
- `17:54` validated the first cache-first hero startup path against install/update ownership concerns and moved the generated `HeroFrameSet` file out of `assets/` into the user cache directory (`$XDG_CACHE_HOME/yam` or `~/.cache/yam`), so runtime startup no longer risks dirtying the repo checkout or writing mutable cache artifacts back into the source tree.
- `18:07` tightened the first hero-cache validation pass with explicit edge-case coverage: added tests proving the cache freshness rule against newer/staler source files, and made the uncached no-`chafa` startup path degrade to a visible placeholder frame instead of panicking, so fresh machines stay bootable while valid cached hero frames remain the preferred common path.
- `18:21` landed the first render-loop reuse slice after the hero-cache work: runtime now keeps one long-lived `Scene` and reuses the boxed layer stack across frames instead of reconstructing it on every draw, which narrows the next optimization seam to per-frame `Grid` allocation and modal-layer short-circuiting rather than scene construction itself.
- `18:34` landed the next render-loop efficiency slice: added a layer-level visibility hook so obviously closed help/move/settings/palette/weather/quit overlays are skipped before `render_to_grid()` is called, with a scene-level regression test proving hidden layers no longer allocate empty grids through the normal render path.
- `18:49` landed the first final-buffer reuse slice in the live runtime loop: the renderer now keeps and clears one reusable composed `Grid` across frames instead of allocating a fresh final frame buffer every draw, which leaves per-layer `Grid` allocation as the next remaining render-loop hot path.
- `19:03` landed the first per-layer scratch-grid slice: the scene renderer now owns reusable scratch grids keyed to the long-lived layer stack, and the simple always-active `field`, `world label`, and `status` layers were moved onto the `render_into_grid(...)` seam so they can draw into reused buffers instead of allocating fresh grids every frame.
- `19:16` widened the first per-layer scratch-grid adoption to the lightweight companion projection layers: `clock`, `weather`, and `date` now also render through the reusable `render_into_grid(...)` seam inside the scene renderer, while keeping the old `render_to_grid()` compatibility path intact for direct layer tests and callers.
- `19:24` moved the always-active hero layer onto the reusable `render_into_grid(...)` seam as well, so hero rendering now participates in the scene-owned scratch-grid path while preserving its existing mask output and the direct `render_to_grid()` compatibility contract for tests.
- `19:41` closed the remaining obvious scratch-grid adoption seams for the always-on scene layers: both the debug overlay and the vine layer now render through `render_into_grid(...)` into the scene-owned reusable scratch buffers, while preserving their direct `render_to_grid()` compatibility path for focused layer tests and any non-scene callers.
- `20:02` added a narrow fast ASCII-only compositor write path and adopted it only where the strings are already guaranteed plain ASCII: the always-on footer, world label, and debug-panel/tab chrome now avoid the general grapheme-aware writer on the hot path, while non-ASCII or mixed-width content still falls back to the original safe text path unchanged.
- `00:14` ran a small post-optimization runtime audit before taking another performance slice: the direct built binary path is now cheap enough that startup is no longer the obvious bottleneck (`./target/debug/yam-rust --version` stayed effectively instant for one run and took about `1.21s` over 200 repeated launches, roughly `6ms` each), while the much slower `cargo run -- --version` path was confirmed to be dominated by Cargo wrapper overhead rather than YAM initialization. The backlog was narrowed accordingly: future performance work should be driven by interactive/profile evidence or by smaller draw-path questions, not another speculative startup pass.
- `00:27` ran one more narrow dev-UI tightening pass after the runtime audit: the code and owning docs now consistently call the lower `[m]ove` surface a `move strip` rather than a `move popup`, so the help popup, scene/rendering contracts, and scene-model notes finally describe the live bottom-docked control surface the same way the app presents it.
- `16:48` bumped the crate and runtime release marker from `0.3.6` to `0.3.7` after the dev-surface cleanup batch: this release carries the compact dev footer, global help/escape modal behavior, the explicit dirty-quit confirmation path, debug-panel tab grouping, the lighter bottom-docked move strip with `Tab`/arrow grammar, the move/help vocabulary cleanup, and the hidden-vine guide suppression fix.

- `11:15` recorded the dev-mode UI cleanup audit without changing runtime code: the active docs/audit/backlog now treat the next maintenance seam as a conservative surface-tightening pass rather than a redesign, call out the still-crowded debug panel and over-visible `calendar (reserved)` seam, and fix the most important docs/runtime drift by aligning the settings vocabulary to `ui`/`features`, clarifying that the currently visible debug surfaces are still dev-gated, and updating the frozen manual boot seed references from the old `(-63, -17)` notes to the current `(-60, -15)` composition baseline.
- `12:03` tightened the dev-surface interaction contract around explicit persisted tweaks: help is now promoted by global `?`, `Esc` backs out of any current dev surface, persisted camera/composition/UI edits stay dirty until explicitly saved instead of writing immediately, and dirty quits now route through a shared-shell quit-confirm modal with save/discard/cancel choices; the owning rendering/architecture/TODO docs were updated in the same pass so the current modal stack and quit semantics stay documented together.
- `12:28` refined the quit-confirm footer into a dedicated decision strip instead of a reused generic modal legend: the modal now centers one plain-text footer line, `[s]ave and quit • [d]iscard and quit • ⎋ cancel`, removes the redundant right-side `? ⎋` hint, and disables confirm-discard on double-tapped `q` by moving that action to `d`; the docs/backlog wording were updated in the same pass so the visible surface and key contract stay aligned.
- `12:40` split the busy debug info panel into a small tabbed readout instead of one long mixed-purpose list: the panel now renders a quiet `runtime / hero / companions / vines` tab row beneath the top scrollbar band, keeps `runtime` as the default compact fact set, and uses `Tab` / `Shift+Tab` for cycling only when the settings modal is closed; matching render/architecture/backlog notes were updated in the same pass so the panel’s new grouping contract is documented where future cleanup work will look first.
- `13:31` removed the leftover dev-only vine shadow fallback from the main scene render path: when `[v]` turns vines off they are now truly absent instead of leaving a dim monochrome trace behind, and the focused vine-layer test was rewritten to lock in full non-rendering while hidden.
- `13:38` followed the remaining “vine shadow” screenshot seam through to the real owner: the lower-right trace was still coming from the debug guide renderer drawing the `main-scene-vine-frame` authoring guides even when the vine organism was hidden. The debug layer now suppresses that guide set whenever vines are off, and a focused render test now blanks every other debug surface so this exact hidden-vine guide leak cannot regress quietly.
- `15:32` tightened the move popup to match the cleaner modal family shape: reduced the shell width, removed the redundant `mode: on` line and body-copy footer instructions, kept the target list inside a more balanced body with matching top/bottom breathing room, and replaced the split footer legend with one centered action line, `[1/2/3/4/5] target • [h/j/k/l] move • [m] or ⎋ close`.
- `16:09` lightened the new move strip one step further after live review: removed the redundant `target:` body line, collapsed the surface to a true three-row band (title, centered target chips, footer), and kept the footer to the minimal move controls so the strip reads as a lightweight docked control bar rather than a small dialog.
- `16:14` narrowed the move strip one more step so the target-chip row has more breathing room against both borders: the docked move surface now caps at a slimmer width with roughly eight cells of inner horizontal padding on each side at the default layout size, while keeping the chips centered and the footer unchanged.
- `16:39` finished the next small dev-UI tightening pass by aligning move/help with the actual live grammar: the help popup now describes `Tab` / `Shift+Tab` plus arrow-key move control instead of the older numbered-target and `h/j/k/l` wording, the runtime no longer keeps those legacy move bindings alive behind the new copy, and the reserved `calendar` seam is now demoted out of the lightweight move strip while remaining explicitly editable in settings.
- `16:47` closed the pre-commit UI audit with one final footer-consistency nit: the help popup no longer pretends `↑ ↓` are active there, so its footer now only advertises the controls that actually apply on that surface.
- `16:58` widened `[?] help` into a true global discoverability surface: it can now be opened from the plain main scene before `dev_mode` is enabled, and the popup copy now distinguishes always-available actions such as quit/help/dev entry from the tools that only become active once dev mode is on.

## 2026-05-12

- `16:58` pinned the repo to the stable Rust toolchain with `rust-toolchain.toml` and explicit `clippy`/`rustfmt` components after confirming the current maintenance/build path does not require nightly; this keeps `yam-rust` aligned with the wider `~/_git` workstation policy and removes silent dependence on whichever global default toolchain happens to be active.
- `09:24` completed the launch-framing freeze for this positioning batch after screenshot confirmation: the default manual camera/home seed is now treated as fixed at `(-60, -15)`, clean launch restores the full default composition seed instead of mixing in stale saved offsets, manual resize preserves the current viewport center, and the default hero anchor family `(-209, -40)` is now treated as the frozen baseline for follow-on companion-positioning work.
- `09:11` widened clean-launch re-seeding from camera-only to the full default composition seed after screenshot review showed the runtime still starting with the old `hero_dy = 0` family: `reset_for_clean_launch()` now restores the entire `UiOffsets::default()` set, so clean boots should finally honor the intended default hero anchor `(-209, -40)` instead of keeping stale saved companion/hero offsets while only resetting the camera.
- `09:02` started the next composition pass after freezing the default camera: moved the default hero anchor seed from `(-210, 0)` to `(-209, -40)` so clean boots begin in the intended vertical family for the hero image instead of leaving the Chafa silhouette almost entirely above the viewport; companion offsets were left untouched for this pass so the next screenshot can show the real remaining clock/date/weather corrections against the now-stable camera and hero seed.
- `08:43` nudged the now-stable clean-boot manual camera seed by one cell to better match the preferred visual framing at the default `124x32` size: changed the default/manual home camera from `(-61, -14)` to `(-60, -15)` after screenshot review, while keeping the clean-launch ownership fix and resize-center behavior intact.
- `08:31` froze the now-confirmed clean-boot manual camera seed at `(-61, -14)` and fixed manual resize behavior so terminal resizes preserve the current viewport center instead of pinning the world to the old top-left camera origin: when the terminal dimensions change, manual pan now recomputes `camera_x/y` from the previous viewport center before clamping, which should keep the current view visually centered during window resizes instead of drifting diagonally.
- `08:13` fixed clean-launch camera ownership so a `--clean` boot now really restores the default manual camera/home seed instead of inheriting a stale saved pan position from snapshot state: `reset_for_clean_launch()` now re-seeds the persisted camera/home offsets and live camera from `UiOffsets::default()` before the boot flow continues, which should stop first-boot starts from landing around `(-61, -3)` when the intended default framing is closer to `(-61, -14)`.
- `07:49` narrowed the second-pass boot correction to a camera-only seed step after screenshot review: moved the default manual camera/home seed from `(-64, -16)` to `(-61, -14)` for the standard `124x32` boot frame so the world framing can be re-evaluated independently before touching the still-unsettled hero and companion offsets again.
- `07:36` started a second default-seed correction pass after inspecting the still-broken first-clean-boot screenshots: reset the clean-launch camera/home seed to `(-64, -16)`, restored the hero's vertical offset to the intended zero-anchor family, and rebuilt the default clock/weather/date/calendar offsets around the target in-world composition instead of continuing to mix the earlier old vertical attachment family with newer camera nudges.
- `07:18` retuned the clean-boot main-scene seed so reinstalls and fresh state start from the intended composed view again: updated the default manual camera/home seed and hero/clock/weather/date/calendar offsets to match the current reference layout more closely, flipped main-scene vines to default-off, and taught the vine layer to keep a dim dev-only shadow render path when vines are disabled so layout work can still see the lower-right world-space guide presence without enabling the full vine presentation.
- `06:55` tightened the live date companion alignment so the rendered Polish date line now self-centers against the combined clock-plus-weather companion span instead of drifting left or right with weekday/month-name length; this keeps the one-line date visually centered between the clock art and compact weather block without changing the existing attachment-offset seam.
- `06:40` evaluated `perkins` as a possible offline tool for doctoring Chafa-rendered hero frames and recorded it in `docs/resource-map.md` as a reference-only experiment: potentially useful for manual cleanup of selected braille-heavy frames, but explicitly not a runtime authority, animation pipeline, or replacement for a future YAM-owned `CellGrid` / frame-cache contract.
- `06:24` shifted the post-audit maintenance posture into future-development resource scouting by adding `docs/resource-map.md` as a dedicated adopt/emulate/reference/reject map for Ratatui ecosystem patterns, Chafa hero rendering, weather providers, ANSI/XBIN/REXPaint source-art workflows, terminal effects, flora-growth references, and theme/palette inputs; also linked it from the front door and docs index so research notes stay out of the active backlog and risk log.

## 2026-05-11

- `18:17` ran a final small handoff-doc cleanup after the sixth external audit: rewrote `docs/release-model.md` around the real current `yam-rust` branch workflow and lightweight release-cut process, made the top of `TODO.md` more action-shaped, and removed the remaining positive coverage note from `docs/audit.md` so the active risk snapshot stays closer to real current risks.
- `18:11` ran a small contract-precision and modal-vocabulary pass after the fifth external audit: aligned the canonical weather brief with the live compact temperature-row format and explicit condition-code-first localization precedence, and moved the last obvious `runtime.rs` raw-meta control checks behind small `UiState` helpers for initial-world forcing, move/settings toggle gating, pointer-probe mode, move-mode key handling, move close, font cycling, and pointer blink activation.
- `17:53` ran a docs-role cleanup pass after the latest green code/test batches: tightened the top of `TODO.md` so the immediate maintenance queue reads more like an actual working set than a pure verification charter, and trimmed `docs/audit.md` back toward a cleaner active-risk snapshot by removing older resolved-history sections that were already better owned by the append-only log.
- `17:41` tightened the weather-asset and clean-launch ownership contracts after the fourth external audit: removed the remaining redundant transient dev-state clearing from `runtime.rs` now that `UiState::reset_for_clean_launch()` owns that reset path, updated the canonical weather brief to match the live `<=15x5` sprite envelope and the `[W]eather` popup's label-plus-five-row review layout, and added atlas-level invariant tests so runtime weather assets stay ANSI-free, five rows high, width-bounded, README-listed, and intentionally mapped across the current `WeatherVisual` set.
- `17:36` ran a narrow modal-policy ownership cleanup after the restored green test baseline: the intent-level dev/popup/settings query predicates that `runtime.rs` was still owning now live on `UiState`, so the input loop reads more like an event dispatcher over named state queries without changing the current boolean modal model or runtime behavior.
- `17:29` finished the first real `cargo test` stabilization pass: tightened the compact weather temperature row so the Polish widget stays inside the width contract again, updated the stale clock/weather/camera tests to assert the current render and clamp contracts, and fixed the border-vine growth state so hitting the max-segment cap also zeros the remaining local tip budget; the repo is now green under both `cargo fmt && bash scripts/check.sh` and the full `cargo test` suite again.
- `17:12` ran a handoff-polish pass after the third external audit: the move/hotkeys/settings surfaces now label the `calendar` companion seam as reserved instead of reading like a live widget target, the front-door README now separates implemented runtime surfaces from the future greenhouse direction more honestly, and the audit/backlog docs were refreshed so the current repo state and next maintenance batch read as active rather than stale.
- `23:40` validated the test-hygiene concern more deeply by running the first full `cargo test` pass of this maintenance sequence: the earlier `WorldFrame` fixture drift was cleaned up, but the broader suite is still not green, with multiple pre-existing assertion failures outside the normal `scripts/check.sh` path and at least one long-running growth test that exceeds a minute; this is now a confirmed follow-up seam rather than a speculative audit note.
- `23:31` ran the broader test-hygiene follow-up from the later external audit: expanded the `WorldFrame` fixture cleanup across the remaining debug/loading/move/settings test modules with small local helper functions, renamed the `scene_config.json` assertion to say `tooling defaults` instead of `runtime defaults`, and corrected the active docs so `F5` and the current `WorldKind::{Boot, MainScene, Sandbox}` vocabulary match the code again.
- `23:16` followed up on the validated external audit with a concrete test-hygiene fix: patched the stale `WorldFrame` initializers in the hotkeys/world-label layer tests to include the live `date` and reserved `calendar` companion fields, and added tiny local render-state helpers there so future companion-frame drift is a little less likely.
- `23:03` continued the runtime-gating cleanup without touching the reserved `calendar` seam: the remaining popup-toggle, popup-close, tab-switch, and free-roam key paths in `runtime.rs` now route through a few named predicates instead of repeating near-identical modal guard expressions inline.
- `22:55` ran the first code-facing maintenance pass on the dev-control seam: consolidated `MetaState`'s repeated "close competing overlays" behavior behind one helper, and tightened `runtime.rs`'s input loop by replacing several copy-pasted dev/settings/move gating expressions with named predicates so the modal contract stays easier to read and less likely to drift.
- `22:43` finished the second pass with one last architecture-note correction so the attachment docs no longer describe the live `date` seam as if it were still reserved alongside `calendar`.
- `22:39` ran a second maintenance pass focused on active-contract honesty: the front-door, weather, scene-model, architecture, and rendering docs now describe the live one-line `date` companion as implemented, keep `calendar` explicitly reserved, update the move grammar to the current `1/2/3/4/5` target set, and stop talking about live weather I/O as if it were still purely future work.
- `22:26` ran a first repo-wide maintenance pass after `0.3.5` and tightened the inspection-surface contract again: stale sandbox/world-space wording was corrected across the front-door and asset/render docs, and the `[W]eather` popup copy/tests now describe the current dev-popup atlas review instead of the older sandbox review path.
- `22:02` cut release `0.3.5` after the Polish weather companion and dev-inspection cleanup pass: the main-scene weather/clock/date cluster now has the warmer BTAS-aligned foreground polish, the compact weather facts and Polish localization are tighter, the date companion is live, and the old sandbox review surfaces now exist as dedicated `[P]alette` and `[W]eather` popups instead of occupying world-space.

## 2026-05-10

- `21:49` polished the compact weather temperature row again so it now reads as ` 8C |  20C`, adding a little more glyph/value breathing room and a clear separator between night minimum and day maximum facts.
- `21:37` restored the old comparative weather-sprite review table as a dedicated `[W]eather` dev popup: the legacy sprite inspection helper is live again, the new modal shell auto-sizes around the atlas table just like `[P]alette`, and the footer/hotkeys/docs now advertise separate palette and weather inspection surfaces without giving up lowercase `[w]` for world cycling.
- `21:22` added one explicit spacer row between the curated and extracted palette sections in the `[P]alette` modal so the source-swatch block starts with a little more visual breathing room under the curated workstation set.
- `21:18` tightened the new `[P]alette` modal shell so it now measures itself from the swatch-sheet content instead of stretching almost full-screen: the popup width/height auto-fit the rendered palette packet, and the inner layout now keeps a uniform 2-row vertical and 4-column horizontal padding around the content block.
- `21:03` moved the BTAS/TNBA palette inspection sheet out of sandbox world-space and into a dedicated dev-mode modal: the old weather-layer sandbox render path is now blank again, a new `[P]alette` popup shows the full curated-plus-source swatch packet in a compact centered shell, and lowercase `[p]` remains reserved for the pointer probe so the older authoring shortcut keeps its muscle memory.
- `18:02` tightened the compact day/night temperature row into two cleaner unit groups (`21C   8C`) instead of the earlier shared-tail format, so the weather panel reads more like two parallel temperature facts than one stitched line.
- `17:56` moved the global footer text onto the extracted neutral-slate foreground `#727C76` so the bottom status line now sits in a quieter BTAS-derived neutral than the brighter secondary text color used elsewhere.
- `17:49` standardized the date line onto the same warm main companion-text style as the clock and weather info instead of leaving it on the dim secondary tone, so the full companion stack now shares one foreground family rather than making the date read lighter/darker by accident.
- `17:41` applied the extracted warm off-white foreground `#DCD8CB` (`yam_neutral_warm_fg`) to the clock/date/weather text path, so the companion stack now reads slightly warmer and closer to the BTAS source sheet while the rest of the broader UI text system still keeps the cooler curated primary foreground.
- `17:28` added the first live one-line Polish date companion in the reserved date seam, using the existing date projection/offset slot specifically as the bridge line between the clock and weather widgets rather than as a separate lower calendar block; the rendered format is now `[dzień tygodnia], DD [miesiąca]` with nominative weekdays and genitive month names such as `poniedziałek, 11 maja`.
- `17:12` confirmed that the live weather refresh seam is still `wttr.in` with static-prototype fallback and changed the default configured location from `Sulkowice` to `Krakow, Poland`, so the runtime's background refresh path and cached weather state now target Krakow explicitly by default.
- `17:03` tightened three live compact-widget presentation details found in screenshot review: the default UI weather locale now starts in Polish instead of English, the day/night temperature row uses the denser `18  5 C` spacing, and the wind row now maps compass directions to matching arrow glyphs such as `↗ ENE` instead of always showing a fixed upward arrow.
- `16:52` reformatted the compact weather facts column around a stricter five-row panel grammar: the widget now uses two rows for localized condition text, a dedicated day/night temperature row driven by parsed daily max/min values from `wttr.in`, a wind row, and a precipitation row that stays blank when there is no measurable rain or snowfall.
- `16:34` polished the Polish weather-localization seam against `wttr.in`'s translation-table wording: `src/weather/text.rs` now keeps provider-code labels closer to the upstream `pl.txt` strings in lower-case widget form, while the visual-only fallback labels remain YAM-owned generalized summaries for cases where no upstream code is available.
- `16:19` expanded the sandbox palette room from a curated-only sheet to a two-group in-world packet: the weather-layer inspection surface now prints both the curated workstation palette and the extracted BTAS source swatches in one compact terminal layout, so palette curation and source provenance can be compared without leaving the YAM sandbox world.
- `16:02` ingested the curated-palette alias-layer proposal into the tracked docs: the shared BTAS/TNBA palette now explicitly distinguishes human display labels, primitive machine-friendly aliases, and YAM runtime semantic tokens, so both dotfiles and YAM can refer to colors like `graphite`, `gray_light`, `green_balanced`, `blue_highlight`, and `amber_dusty` without confusing those stable palette handles with app-specific theme roles.
- `15:43` redirected the sandbox inspection surface from the old weather-only atlas table to a full in-world BTAS/TNBA palette sheet: the theme layer now exposes a compact palette render helper, the sandbox weather-layer path now prints the curated workstation palette directly into the YAM sandbox world, and the docs/tests now treat that world as a live color-review room as well as a sprite/layout review space.
- `15:20` imported `docs/btas_tnba_color_system_canonical_packet.pdf` as YAM's adopted shareable palette reference asset and linked the local palette docs to it, so the runtime repo now carries the current human-facing packet alongside the Markdown guides that explain its provenance and usage boundaries.
- `14:00` added `docs/palette-sheet-reference.md` so YAM now points explicitly at the shared Markdown source for the revised two-page palette sheet, keeping the printable/shareable color reference discoverable from the runtime repo without duplicating the whole sheet content locally.
- `13:33` added `docs/palette-reference.md` as YAM's human-friendly palette guide so theme/weather/flora work can refer to one accurate summary of the extracted-source -> curated-palette -> runtime-semantic chain without treating older one-sheet palette interpretations as canonical runtime truth.
- `08:05` tightened the weather theme seam without reopening the frozen sprite-art pass: the shared palette now exposes explicit weather tokens for rain, heavy rain, sun core, sun ray, lightning, and alert roles; the weather atlas now maps heavy-rain families and thunder-family lightning through those semantic handles instead of reusing broader generic accents; and the docs now record that distinction as part of the weather/theme contract.
- `06:17` prepared a dedicated Moebius handoff sheet for full-atlas manual sprite work: `src/weather/assets/source-art/plain-text/moebius-matrix-v1.txt` now contains the full promoted weather matrix as one fixed-order plain-text composition, `moebius-matrix-v1-legend.md` records the tile geometry and state order for round-trip readability, and the weather asset tree docs now point at that source-art path as the one-shot recolor/tweak sheet for the next manual phase.
- `06:49` documented the first Moebius export inspection result and tightened the round-trip contract around it: `.xb` is now the preferred preserved artifact for full-sheet weather editing because it keeps the full atlas composition, recolors, and glyph tweaks intact, while `.ans` remains a secondary export path that may still help with terminal inspection but can spatially degrade the dense atlas sheet.
- `07:14` froze the current weather/widget pass for cleanup and release prep: the atlas now has the first promoted monochrome sprite batch, the Moebius round-trip workflow is documented, recent cloud/thunder role-assignment drift has been corrected, and the runtime package version was bumped to `0.3.4` ahead of commit/push.

## 2026-05-09

- `00:14` normalized the weather-widget presentation stack so it is less coupled to the current compact screenshot form: split weather presentation into explicit sprite-atlas, localized-text, and layout seams; made the live weather layer render from normalized `WeatherVisual` state plus explicit locale/layout settings instead of provider-origin wording; added an initial English/Polish condition-label path; and updated the weather/architecture/front-door docs so future art and layout work can iterate without re-owning provider parsing or cache logic.
- `08:28` extended the companion-infrastructure seam so future `date` and `calendar` surfaces can be added cleanly without deciding too early whether they should render as independent sibling widgets or as clock-cluster features: the hero-scene attachment model, render-state projection facts, runtime move-target grammar, and settings-position offsets now all reserve explicit `date` and `calendar` slots, while the docs describe them as structural provisions rather than already-live rendered widgets.
- `08:43` reshaped the live compact weather presentation toward the `wttr.in` panel grammar requested during review: the widget now uses a five-row left-sprite/right-facts layout with condition, temperature band, wind, visibility, and precipitation lines; the normalized weather model now carries visibility; and the atlas/layout/text seams remain intact so the `wttr`-like structure is still a YAM-owned presentation rather than pasted third-party output.
- `08:58` hardened the weather-art pipeline and formalized sandbox inspection as the weather visual-review space: compact weather sprites now come from compile-time plain-text assets instead of hand-escaped inline strings, the atlas parser resolves role spans through character-safe slicing so UTF-8 glyphs cannot break on byte boundaries, and the sandbox world now renders a comparative sprite table for atlas/layout inspection without interfering with the main-scene weather widget.
- `09:48` tightened the weather presentation toward a truer `wttr.in` column rhythm after screenshot review: the live compact widget now reserves a fixed-width sprite column so the condition/fact stack starts at one stable second-column edge across every row, and the sandbox atlas review now renders a denser four-column full sprite table so comparative weather-state inspection fits inside the standard sandbox viewport.
- `10:02` investigated the remaining atlas gaps after sandbox review and confirmed they were real mapping omissions rather than render bugs: `Mist`, `Snow`, and `Sleet` had still been falling through to the `Unknown` sprite branch, so the weather atlas now gives those states dedicated local assets and role maps, keeping the sandbox table aligned with the real `WeatherVisual` coverage while following the `wttr.in` / `wego` sprite grammar more closely.
- `10:29` widened the weather normalization/localization seam to follow upstream `wttr.in` more faithfully: the parser now maps condition codes into a fuller family set modeled after `wttr.in` / `wego` (`VeryCloudy`, shower variants, snow variants, sleet variants, and thunder variants), the sandbox atlas review now exposes that broader family list instead of only the earlier coarse set, and Polish condition labels now prefer upstream `wttr.in` translation wording by code before falling back to YAM's own per-visual labels.
- `10:41` turned the sandbox weather review from a flat atlas dump into a grouped sprite-grammar sheet: the sandbox now presents hard comparison families such as base sky states, cloud mass, atmosphere veil, rain grammar, snow grammar, sleet grammar, and thunder grammar in separate labeled rows, so weather-art decisions can be made by silhouette/overlay contrast instead of by scanning an undifferentiated state wall.
- `11:16` extracted the one actually useful weather glyph from a local ANSI art file and imported it as a dedicated `partly cloudy` asset: the ANSI file mostly contained terminal-control noise plus a single manually colored sun/cloud icon, so YAM now keeps that shape as plain text in the atlas and reproduces its authored look through semantic roles (warm sun strokes with a blue cloud accent) instead of treating the raw ANSI file itself as a reusable atlas format.
- `11:34` recorded the future Moebius/XBIN sprite-authoring path as an offline manual-art note rather than active runtime work: the backlog and weather-widget brief now explicitly keep XBIN-style editors on the future art-workflow side while preserving YAM’s current runtime contract of plain-text sprite assets plus semantic color-role mapping.
- `12:02` ingested the latest sprite-design consult into the canonical weather brief as the first manual-art batch: the weather docs now lock the current seed grammar around `clear`, `partly_cloudy`, `cloudy`, and `unknown`, treat the dense current `mist` as a likely `fog` candidate, and stage `clear_night`, `very_cloudy`, `overcast`, lighter true `mist`, and the first stronger precipitation variants as the next offline tweak-and-color pass.
- `15:52` imported the first plain-text monochrome prototype batch into the repo without collapsing alternatives: `src/weather/assets/` now has its own local `README.md` as the mother-doc for live runtime shapes versus manual source-art notes, and `prototypes-v1.md` preserves the full proposed sprite set with all per-state variants and tentative picks for future manual weather-atlas selection.
- `15:56` reorganized the weather asset tree for cleaner sprite hygiene: live atlas shapes now live under `src/weather/assets/runtime/`, monochrome manual-design notes live under `src/weather/assets/source-art/plain-text/`, and a reserved `src/weather/assets/source-art/colored/` track now exists for future hand-colored references without blurring runtime inputs and offline source-art.
- `16:20` promoted the first manual weather-sprite selection batch into the live runtime atlas: the weather runtime now uses dedicated plain-text shapes for `clear_night`, `very_cloudy`, `overcast`, `mist`, `fog`, the first distinct rain/snow/sleet families, and the first thunder/storm variants, while `sunny`, `partly_cloudy`, `cloudy`, and `unknown` remain the anchor shapes of the current monochrome grammar.

## 2026-05-08

- `06:37` inspected the main-scene vine control path and confirmed the current startup behavior: normal launches reset live metamechanics state, so `[v]`-toggled vine visibility snaps back to enabled unless `--preserve-ui-state` is used; then added a dedicated `features` settings tab with a persisted main-scene vine visibility mode (`on`, `off`, `last`), taught clean launch to preserve that policy while still clearing transient dev/modal state, and kept `[v]` as the quick live toggle while persisting its last state for `last` mode.
- `06:43` tightened the boot loading-screen alignment slightly by nudging `press [space] to continue` one cell to the right relative to the centered wordmark/version assembly, while keeping the rest of the loading layout unchanged.
- `06:49` added a dedicated root-level `known_issues.md` tracker with a timestamped issue-entry contract, explicit coordination with `README.md`, `docs/README.md`, `TODO.md`, and `docs/LOG.md`, and a lightweight tag grammar for status/severity/surface/system; seeded it with the still-open minor UI alignment note for the boot `press [space] to continue` prompt.
- `06:58` replaced the shared modal popup frame with continuous box-drawing borders (`┌ ┐ └ ┘ ─ │`) and simplified top-border titles so settings, hotkeys, and move now all use the same cleaner shell without the older dashed ASCII outline.
- `07:10` added a shared bottom-border legend slot to the modal shell and wired compact popup-specific hotkey hints into settings, hotkeys, and move so the modal family now exposes a minimal footer legend instead of keeping all interaction cues in body copy alone.
- `07:18` refined the modal footer-legend spacing so the hint block sits with visible breathing room inside the bottom border rather than touching the border run directly, and changed the settings enter hint to the JetBrains-Mono-friendly `⏎` glyph after the earlier symbol read poorly in live screenshots.
- `08:20` repaired offline install hygiene in `scripts/update.sh`: removed the unconditional `cargo update` network hit from the normal `yam-install` path, switched the rebuild flow to `--locked --offline` first for both `cargo check` and `cargo install`, and only retry the network path if the local Cargo cache is incomplete, so routine reinstalls now behave sanely when crates are already cached but the machine is offline.
- `18:47` began aligning YAM's runtime theme with the repo-wide BTAS/TNBA palette reference: remapped the core panel/text/selection/debug/vine/marker tones away from the older brighter BTAS swatches toward the calmer TNBA-inspired neutrals, blues, greens, and warm accents; expanded the semantic palette aliases accordingly; and updated `docs/theme.md` so the active color contract now points at the BTAS/TNBA reference direction instead of the earlier Pantone-like bundle alone.
- `19:06` extended the settings modal `ui` tab with two more persisted visibility toggles that match the live debug surfaces: camera scrollbars can now be enabled/disabled independently from the world overlay guides, and the debug info panel text block can be hidden without turning off the rest of dev-mode diagnostics.
- `19:14` fixed clean-launch state hygiene so the `ui` tab visibility preferences now truly survive between sessions and reinstalls: world frame, axis, datum, scrollbars, and debug info panel choices are still stored in `~/.config/yam/state.json`, but clean startup now preserves those persisted flags instead of resetting them back to defaults while still clearing transient dev/modal state.
- `19:21` split the shared modal shell chrome from the popup text styling and moved the box-drawing borders onto the BTAS/TNBA `too dark` green `#1F3B2C`, so settings and the other popups now use a quieter green frame while keeping title/body/footer text in the lighter UI foreground.
- `19:24` lightened the popup border chrome immediately after live review: the shared modal shell now uses the subtler green `#2F5A45` instead of the earlier too-dark `#1F3B2C`, keeping the BTAS/TNBA direction while improving legibility against the panel background.
- `19:29` added a separate subtle navy-blue accent path for popup footer control glyphs: the shared modal footer renderer now colors `↑ ↓ ← → ⇥ ⏎ ⌨ ? ⎋` in the muted navy accent while leaving spacing and divider text on the lighter foreground, so the control legend reads more intentionally without turning into a full-color strip.
- `19:32` tightened the popup footer color grammar after live review: the inline `──` separator between control groups now follows the same subtle green border-chrome color as the modal shell instead of staying on the light text color.
- `19:37` refined the popup title treatment on the top border: labels like `settings` now use two-cell padding on both sides and the exact subtle navy `#243B73`, matching the footer-symbol accent instead of the plain light text style.
- `19:40` rebalanced the shared popup accent after another live look: the footer symbols and popup title now use the brighter balanced blue `#2C4C8A` instead of the darker navy, which keeps the BTAS/TNBA blue family while reading more clearly against the modal background.
- `19:45` warmed the settings-tab labels to a BTAS/TNBA red pair after live review: inactive tabs now use auburn `#8A3B2E`, while the active bracketed tab uses brick red `#A33A32`, giving the tab row a clearer hierarchy without affecting body text or row highlights.
- `19:49` toned the active settings-row highlight back down after the tab-color pass: the selected-row background now uses navy `#243B73` instead of the brighter balanced blue, so the highlight stays legible without competing as strongly with the new blue title/footer accents.
- `19:52` adjusted the inactive settings-tab labels again after live comparison: they now use rust `#B24E2E` instead of the darker auburn, while the active tab remains on brick red `#A33A32`.
- `19:58` documented the resulting popup-shell palette more explicitly in `docs/theme.md`: modal border chrome is now the subtle green `#2F5A45`, popup title/footer symbols use balanced blue `#2C4C8A`, inactive settings tabs use rust `#B24E2E`, the active tab uses brick red `#A33A32`, and the selected settings row background uses navy `#243B73`.
- `20:01` updated the repo front door in `README.md` so it now mentions the current popup/settings surface, the persisted `ui` / `features` settings ownership, the state file location, and the BTAS/TNBA popup-theme direction instead of leaving that context buried only in deeper docs and the log.
- `20:22` prepared the attachment seam for a future weather widget by promoting the old hero/clock pairing into a broader hero-scene companion attachment: `weather` now exists as a sibling world-pinned companion with its own persisted offset, move target, positions-tab row, render-state projection helper, and debug readout, while docs now describe weather as parallel to clock rather than as a clock sub-widget.
- `20:49` consolidated the weather-widget concept work into a canonical repo doc: added `docs/weather-widget.md` as the single design brief for provider-neutral weather state, `wttr.in`-first sourcing, YAM-owned Ratatui sprite rendering, BTAS/TNBA weather color roles, and staged rollout expectations; then linked that ownership contract from the front-door README, docs index, architecture contract, rendering contract, and theme contract so future implementation can build against one stable source of truth instead of scattered desktop notes.
- `21:22` tightened the weather-widget brief with two more composition constraints before implementation: the default main-scene weather companion should stay roughly clock-width at about 24 cells, and the widget must treat main-scene aesthetics as a first-class concern by combining minimalist sprites with clear text/number facts while remaining configurable in palette application and internal layout rather than freezing one hard-coded presentation.
- `21:29` moved the weather work from pure specification into a first live runtime scaffold: added a dedicated `weather/` module with normalized model/provider/render boundaries, introduced a compact main-scene `WeatherLayer` rendered through native Ratatui spans from a static prototype snapshot, kept the widget clock-adjacent at roughly 24 cells wide, and updated the front-door/runtime docs so the repo now reflects that the weather seam is structurally live even though real provider/network fetching is still intentionally deferred.
- `21:34` tightened the first weather scaffold into a better runtime ownership shape: the weather layer no longer constructs its own provider snapshot during render, `UiState` now owns a cached weather snapshot plus refresh cadence, runtime refreshes that cache outside the render path, and the settings-editor path now correctly commits `weather offset` row edits instead of silently ignoring them.
- `21:56` advanced the weather seam from pure prototype caching to the first real live-data path: added a `wttr.in` parser/provider boundary, moved refresh onto a background worker so weather fetches stay out of synchronous frame rendering, kept `UiState` as the cached-snapshot owner, and preserved a static prototype snapshot as the fallback path when live fetch fails so the widget remains stable offline or during provider errors.

## 2026-05-07

- `13:08` reviewed the entire loading-screen effort and consolidated the active documentation in `docs/loading-screen.md`: the note now records the three visible loading states, the real staged boot sequence, footer timing, prompt-vs-bar continuity, the empty post-dissolve hold, the unresolved prompt color-drift problem, and the explicit decision to pause manual wordmark tweaking until a stronger figlet/offline inspection workflow exists.
- `13:11` cut release `0.3.3` for the loading-screen and figlet pass: canonical launch now enters a staged boot world with transparent loading art, shared FIGlet-backed wordmark support, world-aware loading transitions, cleaner footer timing, prompt/dissolve continuity, and a documented pause on unresolved wordmark/color-shift polish.

## 2026-05-06

- `15:19` split the world debug overlay into two real surfaces instead of one coupled drawing pass: the rectangular world frame/border and the datum-centered crosshair/axis can now be toggled independently, and the settings modal gained a dedicated `ui` tab with row toggles for both
- `15:20` made the settings modal more world-aware by keeping the new `ui` overlay controls available across worlds while continuing to hide main-scene-only hero/clock position and gif controls in sandbox
- `15:33` extended the `ui` tab with a third overlay toggle for the world datum marker at `(0, 0)`, keeping the datum independent from both the frame and the axis instead of folding it into either surface
- `15:35` tightened the popup affordance flow so `Esc` now directly closes the settings and move popups, while still canceling buffered settings-field editing before it dismisses the whole settings modal
- `15:48` cut release `0.3.2` after the settings-modal maturation pass: world-aware settings, independent world-overlay toggles for frame/axis/datum, darker BTAS row highlight polish, and better popup-exit behavior are now part of the release surface
- `16:02` added a reusable loading overlay seam to the UI stack: `UiState` now owns a short-lived loading state, a dedicated loading layer renders a centered progress/spinner popup, and world switching now uses that overlay as the first live transition path instead of jumping instantly with no visual handoff
- `16:18` promoted `yam` to the canonical launcher shape: it now prefers the live repo source at `~/_git/yam`, opens in a new Ghostty window on the current macOS setup, and relies on the in-app loading screen instead of printing runtime identity before the TUI takes over; `yam-sandbox` follows the same launch pattern for the sandbox world
- `16:31` tightened canonical launch polish after a live sanity check: the loading layer now owns a full-screen backdrop instead of appearing as a popup over an already-visible scene, and `yam`/`yam-sandbox` pass a clean-launch flag so ordinary startup enters the boot screen from a non-dev UI state
- `16:44` changed the loading treatment to the lighter boot-screen direction: startup now renders the loading text as transparent ASCII content over a dedicated empty boot world instead of using the modal shell/background path, leaving later color and Tachyonfx motion as explicit future polish rather than hardwiring them now
- `16:57` completed the boot-world loading implementation: `WorldKind::Boot` now builds a genuinely empty scene for startup, the transparent ASCII loading art renders version/build plus the text bar over that scene, and the launch/runtime state matches the documented “boot first, main scene after” handoff instead of leaking partial main-scene composition underneath
- `17:09` made clean non-dev boot the runtime default instead of an opt-in launcher behavior: fresh startup now resets dev/modal UI state before the loading screen and first world render unless `--preserve-ui-state` is explicitly requested for diagnostics
- `17:18` hardened fresh-boot cleanliness after a live screenshot check: runtime now explicitly clears dev/modal state on clean launch and dev-only layers/footer expansion stay suppressed while the loading screen is active, so the boot world no longer exposes stale debug surfaces during startup
- `17:27` moved the loading-screen isolation into the scene pipeline itself: when loading is active, only the loading layer and the minimal footer are rendered, which prevents debug/world overlay leaks during boot even if stale UI state somehow survives elsewhere
- `17:46` upgraded startup into a staged boot sequence with `tachyonfx`: boot now uses `coalesce` for the transparent ASCII loading art, runs the existing bar animation for three seconds, waits silently for `[space]`, and then uses `dissolve` before handing off to the first world; ordinary world-switch loading keeps its shorter transition path instead of inheriting the blocking boot ceremony
- `18:02` moved the boot/loading ASCII art out of Rust string literals and into `assets/loading_art.txt`, with the loading layer now consuming the repo-tracked text asset directly so shape tweaks can be reviewed in-YAM without fighting escape sequences in source
- `18:19` paused manual loading-wordmark tweaking after another in-YAM screenshot still showed visible glyph/layout glitches; added a dedicated `docs/loading-screen.md` note to preserve the boot-sequence architecture, asset ownership, successful decisions, failure modes, and future resume checklist so the next pass can restart from documentation instead of rediscovering the same issues
- `18:42` studied `sigye` as a reference implementation and recorded the useful transfer ideas in `docs/reference-sigye.md`: a dedicated FIGlet parser/registry/render subsystem, transparent direct glyph rendering that skips spaces, and compact styled hotkey hints like `↑↓ nav  ←→ change`; the corresponding YAM follow-ups were added to `TODO.md` instead of rushing directly into implementation
- `19:07` replaced the one-off loading-art asset path with a real shared FIGlet subsystem: bundled `.flf` assets are now embedded through `render/fonts.rs`, the clock still uses the same UI-facing font control but resolves through the new registry, and the boot wordmark now renders `yam` from the Standard FIGlet font instead of depending on `assets/loading_art.txt`
- `19:19` tightened loading-screen block alignment after live screenshot review: the caption, FIGlet wordmark, version line, and all loading labels are now centered explicitly per line rather than relying on embedded left-padding, and the loading bar is intentionally nudged two cells to the right so boot, sandbox, and main-scene loading states share one stable visual centerline
- `19:32` refreshed the loading-bar visual language: boot and transition loading now render `■` for fulfilled increments and `⋄` for remaining increments while keeping the current bar length, replacing the older alternating ASCII punctuation bar with a clearer progress/readiness cue
- `19:46` refined the boot loading handoff so the finished bar state no longer keeps pretending to load: once boot reaches the wait-for-space phase, the loading label and progress bar are replaced with a single centered `press [space] to continue` prompt with empty breathing rows above and below it
- `20:04` refined the boot wait-for-space moment: the minimal `[q]uit • [d]ev` footer now appears only once loading has actually finished and the `press [space] to continue` prompt is visible, and that prompt now pulses subtly from the default light text toward BTAS green so the boot screen reads as alive rather than hung while it waits for input
- `20:11` stabilized the vertical rhythm of the boot handoff state by giving `press [space] to continue` the same two-row gap below the version line as the active loading label, so the loading block does not appear to jump or collapse when the bar phase hands off to user confirmation
- `20:22` tightened the wait-for-space prompt animation so the foreground now shifts on a clear 3-second white→BTAS-green→white cycle using a smooth ping-pong interpolation with a softer BTAS-green midpoint and a stronger green endpoint, making the prompt feel more visibly alive without turning it into a blink
- `20:36` fixed the final boot-phase continuity bug: the visible `press [space] to continue` state now remains on-screen through the dissolve phase (and takes the minimal footer with it) instead of snapping back to the older `loading...` composition just before exit
- `20:37` documented that the current wait-prompt color drift is still visually broken: although a white-to-BTAS-green foreground shift exists in code, the live terminal output does not show it clearly enough to treat the effect as working, so it remains an explicit future-fix item rather than finished polish
- `20:44` added a short post-dissolve hold to the boot sequence: after the final prompt dissolves away, YAM now rests on the empty boot world for 0.5 seconds before handing off to the first real world, giving the launch sequence a cleaner visual exhale instead of an immediate scene jump
- `20:52` fixed the post-dissolve boot hold so it now truly rests on an empty boot screen: the new `Hold` phase had been added to the loading state machine but the loading layer still treated it like an older loading phase, so the final 0.5 second pause was showing stale loading content instead of a genuinely empty boot world
- `12:51` clarified the log contract explicitly: new `docs/LOG.md` entries should use the local system time of the active machine, which for the current setup means `Europe/Warsaw` with seasonal offset changes such as `CEST` / `UTC+02:00`, while the visible inline format remains compact `[HH:MM]` local time

## 2026-05-05

- `12:01` added a dedicated vine implementation roadmap to `docs/vines.md`, summarized the execution checkpoints in `TODO.md`, and kept the route focused on world-owned flora state, guide-derived geometry, read-only rendering, and HUD/footer invariance.
- `12:25` completed the Phase 0 vine-readiness validation pass: targeted tests for `core::spatial`, `scene::coords`, `render::render_state`, `scene::layers::status_layer`, and the key resize-round-trip scene cases stayed green, and the vine docs/audit now record that the current gate is validated while the remaining caution stays with spatial-layer consolidation.
- `12:27` completed Phase 1 storage-only flora work by adding `core::flora`, introducing a world-owned `flora: FloraState` field on `WorldState`, and pinning the inert vine data shape with focused `core::flora` and `core::world` tests while keeping rendering and UI behavior unchanged.
- `12:29` completed Phase 2 by seeding one deterministic border-vine instance into `WorldState::new()` through `core::flora`, keeping it invisible with no segments or organs and one dormant growth tip, and verifying the seed shape with focused `core::flora`, `core::world`, and repo check-script coverage.
- `12:32` completed Phase 3 by adding a pure `core::flora` guide-set lookup helper that reads named sets through `SpatialGuideIndex`, emits ordered world-space paths for enabled line/polyline guides, skips missing or unsupported shapes cleanly, and stays entirely out of rendering while focused `core::flora`, `core::spatial`, and repo hygiene checks remain green.
- `12:49` completed Phase 4 by adding a static main-axis derivation helper in `core::flora` that turns guide paths into deterministic `VineSegment` geometry, keeps the vine's world-space root attachment as the explicit origin authority, records guide-set/guide associations on the derived axis, and remains render-free while focused flora tests and the repo check script stay green.
- `12:55` completed Phase 5 by adding a read-only `VineLayer`, realizing one boot-time static border-vine axis from the named guide set in world state, proving the vine layer renders world-attached geometry without writing into the footer row, and recording the visual direction as `cbonsai`-influenced but not bound to strict imitation.
- `13:48` completed Phase 6 by extending the existing dev/debug panel with read-only vine inspection facts for count, identity, axes, segments, tips, and guide-set provenance, keeping the surface subordinate to `dev_mode` and leaving footer/modal behavior unchanged while focused debug-layer tests and the repo check script stayed green.
- `14:04` completed Phase 7 by landing a small deterministic growth rule in `systems::growth`: the boot vine now extends one segment on a fixed cadence from its active tip, stops at an explicit segment cap with a spent tip and mature life state, keeps render read-only, and stays covered by focused growth tests, the updated debug-panel fact test, and the repo check script.
- `14:27` corrected the shared Cartesian projection boundary so world `y` now rises upward while screen `y` still rises downward, threaded viewport height through the projection helpers and world-render callsites, tightened the affected coordinate/render/layer tests to the new contract, and recorded the vine-facing projection note in `docs/vines.md`.
- `14:39` corrected the follow-on orientation regression from the projection change: the hero layer now maps raster rows to decreasing world `y` so the chafa sprite is no longer vertically flipped, and the dev movement helpers now treat `up` as `+1 y` in world space for hero/clock offsets, pointer probes, and camera motion.
- `14:51` reserved the bottom terminal row exclusively for the footer by shrinking the active world viewport to `frame height - 1`, which lifts the framed world content one row upward without changing the footer contract.
- `15:02` nudged the debug info panel one additional row upward and moved the boot vine seed/root to `(104, -27)`, translating its fixture guides with it so the realized border-vine axis still stays coherent from the new origin.
- `15:09` retuned the boot vine guide fixture so the seeded axis now grows leftward and upward from `(104, -27)` toward roughly `(50, 0)`, making the default vine read as reaching toward the clock rather than drifting away from it.
- `15:21` resumed normal vine work with the first old-branch-inspired lifecycle slice: `VineGrowthTip` now tracks local age and remaining growth budget, and the growth system now spends that budget deterministically so a tip can become spent because its own lifespan is exhausted, not only because a global segment cap was hit.
- `15:31` added a debug-mode vine visibility toggle on `[v]`, persisted it in `MetaState`, taught the `VineLayer` to honor it directly, and exposed the hotkey in the dev footer so vine rendering can be quickly hidden without disturbing world state or growth.
- `15:44` resumed vine anatomy with the first explicit organ slice: segment aging now runs in `systems::aging`, mature odd-indexed segments rebuild attached leaf organs into vine state, the vine layer renders those leaves as tiny direction-derived marks, and the debug surface now reports organ count alongside axes and segments.
- `15:53` tightened the stem rendering contract so the main axis no longer borrows the generic soft-line grammar: vine stems now choose continuity-first structural glyphs from local path connectivity, trunks get a heavier variant of the same language, and focused vine-layer tests pin the new stem/trunk policy.

- `05:14` continued the stability-first maintenance pass with a fresh day entry so the append-only log can keep the coordination, audit, and backlog contracts moving forward in a clean timestamped shape
- `06:58` aligned the audit priority wording with the front door and docs map so the stability-first contract now reads consistently across README, docs/README, and docs/audit
- `07:18` made the audit priority line match the front-door wording exactly so the hero/flora stability-first contract reads the same across README, docs/README, and docs/audit
- `07:25` fixed a real spatial compatibility bug: projected world and anchor paths were collapsing signed off-screen coordinates too early, so `scene::coords` now keeps a signed `ScreenPos`, projects world/anchor elements through the proper screen helper, and leaves screen-attached values camera-invariant; the full `scene::coords` test group is green again
- `07:29` added a vines readiness gate to the audit and backlog, removed stale spatial `node` wording from the guide-capture flow, and cleaned a duplicate spatial API bullet so vine work has clearer preconditions before implementation begins
- `07:37` updated the footer row regression test to match the current no-band footer contract: only written footer text carries the BTAS foreground style, while unwritten cells remain plain
- `07:54` reran the full Rust test suite after the vines-readiness and footer-test cleanup; `cargo test --quiet` is green with 83 passing tests
- `10:05` added `docs/vines.md` as the pre-runtime vine ownership contract and linked it from the README, docs map, audit, backlog, scene model, and rendering contract so vine work has one place for readiness and ownership rules before implementation begins
- `10:21` completed a pre-commit hygiene pass for the spatial/theme/docs/vines-readiness batch: `scripts/check.sh` passed and `cargo test --quiet` passed with 83 tests
- `16:11` pivoted the drawing work back into a shared core engine seam: added `render/drawing.rs` with reusable path-stroke, glyph-stamp, and occupancy-mask primitives, rewired the vine layer to consume those primitives, and documented the engine role in the architecture, rendering, and vines contracts
- `16:34` established the first explicit multi-world seam: `WorldState` now boots by `WorldKind`, the main scene and a sparse sandbox world are separated, dev mode can cycle worlds with `[w]`, the debug surface reports the active world, and the pointer probe is now sandbox-owned so point-to-point drawing trials have a dedicated lab surface
- `16:49` promoted the sandbox into a first-class launch path: added the `yam-sandbox` wrapper, taught `yam-rust` to accept `--sandbox`, updated install/runtime docs, and aligned the expected routine around `yam-install` followed by either `yam` or `yam-sandbox`
- `17:27` fixed the install-path mismatch for the new sandbox launcher: `scripts/update.sh` now installs both `yam` and `yam-sandbox` into `~/.local/bin`, so the common `yam-install` rebuild routine can actually expose the new wrapper instead of only refreshing `yam-rust`
- `17:38` tightened the sandbox model so it behaves like an internal alternate space instead of a half-empty main scene: hero and clock layers now render only in the main scene, the dev footer only advertises the pointer inside sandbox, the debug surface reports hero/clock facts as `n/a` there, and the docs now describe sandbox as a switchable in-app world while `yam-sandbox` remains only a convenience preset
- `17:46` added a dedicated world-label overlay in dev mode so both spaces now announce themselves with centered top-row labels: `MAIN SCENE` for the default composition and `SANDBOX` for the dry-trial world
- `17:59` investigated the apparent scene-layer test hangs and found they were dominated by eager hero GIF/chafa rendering inside `UiState::new()`; test builds now use a lightweight hero stub so UI/layer tests exercise the real state seams without paying the full runtime render pipeline
- `18:11` restored the pointer probe as a shared dev-mode tool in both worlds after over-constraining it during the sandbox split: `[p]ointer` is back in the main-scene footer, the runtime hotkey is no longer sandbox-gated, and `UiState::toggle_pointer_probe()` again works across both the main scene and sandbox
- `18:24` re-encoded the default main-scene hero and clock start positions to match the desired fullscreen composition, and recorded that these boot-time offsets should eventually move from `UiOffsets::default()` into the tweakable settings layer instead of living forever as hard-coded layout constants
- `18:38` gave the settings modal its first CDDA-like list behavior: `Tab` still cycles tabs, `Up/Down` now moves a per-tab active row cursor, and the selected settings row is rendered with a clean background highlight so the popup reads like a navigable list instead of a static fact sheet
- `18:53` turned the `positions` tab into the first real buffered editor: labels now read `x = ..., y = ...`, `Enter` opens and commits row edits, `Left/Right` switches the focused axis field, numeric input edits the active field, and `Esc` cancels without mutating runtime values
- `19:02` made the positions editor respect fullscreen/world-fit constraints: when the active viewport already covers the full world, the camera row now renders as locked/disabled and `Enter` will not open camera editing, matching the existing no-scrollbar/no-meaningful-pan behavior
- `19:09` simplified the locked camera row copy to a passive `camera: [locked in fullscreen]` status line so the disabled state no longer pretends to expose editable `x/y` fields
- `19:14` polished the locked-row presentation so the selected disabled camera row now uses one coherent muted-grey-on-blue style instead of splitting the row highlight from the text color

## 2026-05-04

- Recorded that the recent hero-rendering test run was unsuccessful and should remain a cautionary experiment rather than a baseline path
- Marked the hero-rendering pipeline as one of the current weak spots: the active Chafa path is stable, but the offline compiler / `CellGrid` direction is still an experiment rather than a replacement baseline
- Ranked the current weak seams in the audit: spatial relation consolidation is the highest-priority gap, hero rendering is second, and the flora runtime is still a broader implementation target
- Added explicit screen-point projection helpers to `core/spatial` so the spatial layer has a clearer world/screen split while retaining compatibility wrappers for the older world-point path
- Narrowed the spatial audit language around anchors: `Space::Anchor(EntityId)` still needs a registry-backed resolution path, so anchor identity remains a planned follow-on rather than a currently-resolved runtime relation
- Made the compatibility layer world-aware for anchor resolution: `scene/coords.rs` now consults `WorldState` entities when `Space::Anchor(EntityId)` is present, so anchor identity is partially real rather than purely declarative
- Clarified that the new anchor lookup remains a compatibility path and that the long-term goal is to move the entity-backed relation logic into `core/spatial`
- Recorded the current Ghostty hidden-titlebar baseline as `window-width = 120`, `window-height = 30`, which opens to about `124x21` usable cells on the current macOS setup
- Updated the Ghostty hidden-titlebar baseline to `window-height = 31`; the current setup now opens to about `124x32` usable cells instead of the earlier `124x21` note
- Lightened the footer contract so the runtime footer is plain text without a green bar and the visible dev hint is now `[d]ev` instead of `[d]ev mode`
- Removed the now-unused footer bar palette/style helpers after the footer switched to plain text, and updated the footer tests to match the shorter `[d]ev` label
- Tuned the footer text to a dimmed BTAS-grey (`Rgb(26, 31, 38)` with dim modifier) so the footer reads lighter while staying readable
- Learned from a footer mismatch that `yam-install && yam-rust` can still surface an older installed binary if the executable hash/build stamp does not change; future visual checks should confirm `yam-rust --version` and build stamp after reinstall before treating a screenshot as current
- Fixed the source-side footer spacing bug after screenshot inspection showed the compact `[q]uit  •  [d]ev` spacing had not actually been applied in `status_layer.rs`; the dev-mode footer hint now uses tighter spacing too
- Reinstalled `yam-rust` through `scripts/update.sh` and confirmed that the build stamp stays stable when the git/source hash is unchanged, so runtime identity alone is not enough to prove a visual refresh unless the footer output is also rechecked
- Removed the footer `DIM` modifier so the BTAS-grey tone can be evaluated without a second styling layer; this should help distinguish a true palette mismatch from a terminal/font perception issue
- Darkened the footer BTAS-grey a little further (`Rgb(20, 24, 30)`) to see whether the footer reads as subdued enough without needing background tinting or dimming
- Temporarily set the footer foreground to an unmistakable magenta diagnostic color so we can verify whether Ghostty and the runtime are honoring footer text styling end-to-end before choosing the final BTAS-grey tone
- Confirmed via screenshot that Ghostty and the runtime honor footer styling end-to-end; restored the footer from the magenta diagnostic to a subdued grey (`Rgb(96, 102, 110)`) now that the color path has been proven
- Final footer lesson: the visible problem was a mix of source spacing drift and style perception, but the render pipeline itself was healthy; the working solution is the compact footer grammar (`[q]uit  •  [d]ev`) with a subdued grey foreground and no background tint or dim modifier
- Codified the BTAS palette as a reusable architecture theme layer with a canonical `BTAS` palette bundle, semantic palette aliases, and shared style helpers so the whole YAM stack can reuse the same visual vocabulary
- Added a dedicated `docs/theme.md` and a canonical `src/theme/btas.rs` bundle so the BTAS palette can be reused as one shared theme contract across panels, modal shells, hero overlays, camera indicators, pointers, and footer text
- Trimmed the footer to a single leading/trailing cell of padding so the compact footer grammar reads tighter without changing the color or the no-background contract
- Moved the footer styling fully onto the BTAS helper path so the status bar follows the same theme contract as the rest of the UI surfaces instead of using a raw palette alias directly
- Folded the remaining obvious debug/UI raw colors into BTAS helpers too: the small debug panel now uses a BTAS debug-text style, while guide traces use a shared BTAS trace style instead of ad hoc green/dark-gray literals
- Documented the remaining raw-color literals as intentional low-level render/test exceptions: hero-art calibration and compositor-level tests may keep precise literals when the goal is to verify rendering behavior rather than define a reusable UI surface
- Tightened the spatial compatibility layer again so `resolve_anchored_position(...)` now returns screen space directly, making the anchor helper explicit instead of forcing another world-shaped hop in the compatibility path
- Continued the spatial cleanup by adding an explicit screen-point constructor in `core/spatial` and using it in the scene compatibility layer so the screen branch is a little less fuzzy without breaking the existing world-point callers
- Tightened the screen compatibility path one step further by introducing `resolve_screen_position(...)` in `scene/coords.rs`, so the screen branch now passes through the dedicated `ScreenPos` type before being bridged back to the legacy world-shaped compatibility return
- Continued the spatial consolidation by adding `SpatialResolver::resolve_anchor(...)` and routing `scene/coords.rs`'s anchor helper through it so the anchor math lives more centrally in `core/spatial`
- Extracted `resolve_anchored_position(...)` in `scene/coords.rs` so entity-backed anchor resolution is named and isolated instead of being inlined inside the `Space::Anchor` match arm
- Added `SpatialResolver::resolve_anchor_or_world(...)` so the shared spatial layer now owns the anchor-or-fallback resolution shape, and `scene/coords.rs` routes the compatibility path through that central helper
- Recorded the current spatial compatibility contract: anchor and screen branches now have dedicated helpers, while `resolve_position(...)` stays as the legacy world-shaped bridge for older call sites until the rest of the tree is migrated
- Added `resolve_element_screen_position(...)` so the compatibility layer has an explicit screen-returning helper, and `resolve_position(...)` now delegates through that bridge instead of rebuilding the screen branch inline
- Added a ranked top-five weak-area summary to the audit: spatial relations remain the top seam, followed by hero rendering, flora runtime, theme convergence, and docs/runtime synchronization
- Reasserted the current project priority: stability and efficiency first, hero GIF aesthetics frozen for now, flora deferred until systematic preparation is in place, and cross-cutting coherence across UI/theme/docs to be improved before new features
- Propagated the stability-first priority into the front door and docs map so the repository overview now explicitly says the active focus is stability/efficiency, with hero aesthetics held steady and flora deferred

- `15:00` tightened the log format guidance so future entries should prefer day headers with inline timestamps while the existing append-only history stays intact
- `15:10` tied the repo coordination contract together across `README.md`, `docs/README.md`, `TODO.md`, `docs/audit.md`, and `docs/LOG.md` so the backlog stays execution-focused, the audit stays risk-focused, and the log stays append-only
- `15:15` cleaned a docs-index duplication so `docs/README.md` now lists `TODO.md` only once while preserving the coordination contract between the front door, backlog, audit, and append-only log
- `15:20` aligned the priority wording in `docs/README.md` with the front-door README so the stability-first contract reads the same in both places
- `15:25` reflected the log-format rule in `docs/README.md` so the docs map now points to the day-header-plus-inline-timestamp shape for future log entries
- `15:30` removed timezone suffixes from the active log timestamps so the append-only history now uses plain `[HH:MM]` inline stamps under day headers

## 2026-05-03

- revised the active architecture docs to describe one canonical spatial relation layer for world datum, relative anchors, guides, masks, and organism guidance, and corrected the flowers typo in the broader plantlife framing
- updated the audit snapshot with the remaining spatial-model split so the current risk notes match the present code layout
- clarified the active coordinate contract so YAM world space is explicitly Euclidean with a centered `(0, 0)` datum, while screen space remains a separate terminal projection
- recorded the Ghostty baseline typography notes in `docs/config.md` so the current font and cell-metric tuning assumptions stay alongside the `124x32` startup size
- noted that HUD/UI layout may still depend on the shared world-spacing model for offsets and alignment, even though final HUD placement remains screen-attached
- split the top-level YAM definition into an implemented-today description and an intended-architecture description so the docs stay honest about what is already coded versus what is still emergent
- surfaced the implemented-vs-intended YAM definition in the repo front door and docs index so the distinction is visible before readers descend into the deeper contracts
- compressed the front-door YAM definition into a single slogan plus a short non-goals list to keep the repo identity concise and scan-friendly
- redefined YAM around two simulation spaces, the main scene visualiser and the greenhouse/lab space, and clarified that flora simulation must support multiple organism anatomies instead of treating vines as a singular special case
- named the concrete main-scene composition and greenhouse concept phases, including the current flora prototypes for tree-stump scaffolding, tropical vines, and a monstera-like plant
- fixed the world/fullscreen size contract to `212x56` world cells plus one reserved footer row in the `212x57` terminal frame, and updated the border/footer tests accordingly
- formalized the renderer precedence ladder so world base, world props, world-tied debug/dev assets, HUD, and modal overlays have explicit overwrite priority instead of relying on incidental z-index behavior
- added an interface contract for keyboard-first muscle-memory controls, everyday-vs-dev debug separation, semantic visual hygiene, and a command-palette discoverability path based on the CLI/TUI research pass
- added an explicit interface mode map for `normal`, `inspect`, `debug`, `dev`, and `command palette` so everyday use, drill-down reading, diagnostics, and editing/tooling stay cleanly separated
- tightened the mode-to-input contract so the docs now distinguish normal, inspect, debug, dev, and command-palette behavior from the concrete dev hotkeys already implemented in the runtime surface
- added an explicit screen-zone contract for the main scene, HUD/footer, debug/inspect, and modal overlay regions so the layout vocabulary stays stable across render and scene docs
- added a visible-content map for the main scene, HUD/footer, debug/inspect, and modal overlay regions so new UI or diagnostic elements can be assigned before they are implemented
- tightened the visible-content map so the footer stays one-line only, debug/inspect stays factual rather than command-heavy, and modal overlays remain the only place for the denser action vocabulary
- added an explicit modal-vocabulary contract so hotkeys, move, settings, pointer, and camera-home actions stay grouped according to the controls that are already implemented in code
- reconciled the UI docs with the runtime input loop so the modal gating, dev-only probe motion, and camera-home actions are described as currently enforced behavior rather than loose future intent
- clarified that the soft-line renderer is meant to cover every world-space line and that the pointer probe is the practical authoring tool for recording precise coordinates for guides, nodes, masks, and other spatial relations
- noted that the spatial model should lean on Cartesian and Euclidean logic where possible so world-space authoring stays practical and easier to reason about
- added an explicit guide capture workflow so the pointer records coordinates first, then points/nodes/lines/masks are composed and validated with the soft-line renderer
- added an explicit capture UI contract so guide placement uses the pointer, debug/info panel, hotkeys, and move mode without turning the HUD into an editor
- added a read-only guide-set preview requirement so guide sets and subsets can be inspected, including mask-outline polylines, without making the UI mutable
- reserved the term `nodes` for plant morphology/anatomy systems for now, and removed it from the spatial guide-capture contract pending further terminology research
- corrected the main-scene scaffold description so the tree-stump hero support is explicitly a Y-shaped fork under the hero GIF
- ingested the flora morphology survey into the live contracts as a hybrid graph-plus-segment model with metamer/internode/meristem terminology and small prototype targets for the first implementation
- added a minimal Rust-shaped plant data sketch so the flora model stays implementable as Plant/Axis/Metamer/Meristem/Organ state with derived geometry
- added a minimal lifecycle sketch so plants can progress through Seed/Growth/Mature/Senescent/Decay while organs advance through their own states and geometry is regenerated from plant state each tick
- clarified that each plant organism is an independent life-form and that YAM likely needs a read-heavy species registry/database layer for species traits, growth rules, and other reusable simulation data
- added the dedicated per-life-form log-journal requirement so lifecycle events and debugging notes can be tracked per organism
- defined the per-life-form journal payload as a compact event log centered on lifecycle transitions, growth steps, organ changes, environmental effects, pruning, and debug annotations

- Defined the species-registry payload as compact reusable metadata for morphology defaults, branching pattern, internode length, leaf distribution, growth rate, tropism rules, lifecycle tuning, allowed organs, and debug labels
- Noted that the state/stat/journal/registry layer may eventually merit its own dedicated render mode alongside the main scene and greenhouse if inspection needs grow
- Noted an alternative per-life-form popup UI strategy for inspecting organism data and making limited tweaks like growth rate or lifecycle length
- Added a simple comparison rule of thumb for the three inspection paths: dedicated mode for registry-scale browsing, popup windows for quick tuning, and lightweight debug overlays for routine checks
- Added a registry taxonomy note grouping species fields into anatomy defaults, growth rules, phenotype ranges, lifecycle tuning, and debug labels
- Added a botanical-basics reminder so species terms and morphology fields stay grounded in real plant structure before they are committed to YAM
- Added a species-entry checklist with fields for species id, display name, habit/form, anatomy defaults, morphology notes, branching pattern, internode length, leaf distribution, growth rate, tropism rules, lifecycle tuning, allowed organs, debug labels, and journal hints
- Added concrete morphological briefs for the current prototypes: the bark-textured Y-shaped tree-stump scaffold, border-aware sprawling vines, and the multi-stem fenestrated monstera-like plant
- Added example species entries informed by botanical consults, with each prototype explicitly labeled as an inference rather than a direct copy of a real species
- Added a botanical species-template split so registry data keeps taxonomic inspiration, support habit, growth mode, leaf architecture, reproductive strategy, life cycle notes, and ecology cues separate from generic fields
- Tightened the three example species into explicit registry-style entries with ids, support strategy, stem plan, growth-tip behavior, branching pattern, organ outputs, lifecycle defaults, registry tags, and inspection notes
- Clarified the scaffold as a pre-rendered dead-or-senescent hero support remnant with only minimal moss growth, stable bark texture, and later vine attachment/overgrowth
- Filled the first species-template examples for the Y-shaped tree-stump scaffold, border-aware vine family, and monstera-like multi-stem plant
- Canonized a plant anatomy and morphology glossary so stem, branch, stub, node, internode, leaf, meristem, axis, insertion, and organ mean the same thing across architecture, scene, and rendering docs
- Added a compact working plant-vocabulary table mapping each botanical term to its YAM meaning and intended use
- Extended the working plant vocabulary with bud, petiole, petal, root, crown, canopy, flower, and fruit so structural, growth, and reproductive language stays distinct
- Noted the project's historical inspiration from `cbonsai` so the plant-structure lineage stays visible as YAM expands into a broader greenhouse and multi-species model
- Added a short inspiration lineage block naming `cbonsai`, `Dwarf Fortress`, and `Cataclysm: DDA` as the main plant/UI ancestry references
- Added a terminology-authority legend so plant terms can be marked as strict, inferred, or provisional
- Added explicit term labels for the current glossary entries so the repo can distinguish strict botanical terms, YAM-inferred terms, and provisional placeholders
- Added a dedicated `docs/glossary.md` so plant anatomy, morphology, and spatial terminology have a single canonical reference page
- Removed duplicated glossary prose from the main contracts and replaced it with a pointer to `docs/glossary.md`
- Expanded the glossary with scene/UI terms for camera, viewport, HUD, suite, overlay, world space, screen space, frame, layout, and panel
- Extended the glossary with layer, zone, modal, footer, status bar, debug overlay, inspect, dev mode, command palette, main scene, and greenhouse terms
- Renamed the glossary to a broader YAM glossary and added `shell` for the shared modal container vocabulary
- Updated the front door to point to `docs/glossary.md` as the shared terminology source of truth and to reflect the current terminology contract more directly
- Added a front-door note saying the glossary owns shared terminology and that duplicate term definitions should not be reintroduced into the contracts
- Trimmed the front-door YAM definition slightly so the README reads more tightly while keeping the glossary/source-of-truth note intact
- Wired the debug guide renderer through `SpatialGuideIndex` directly and updated the contracts to say the shared guide index is now a live runtime consumer, not just a migration target
- Broadened the docs index label for `docs/glossary.md` so it reads as the YAM terminology source of truth rather than only a plant/spatial glossary
- Recorded Lua as a bounded optional scripting/plugin layer for species authoring or debug/dev tools, while keeping Rust as the host for canonical state and render ownership
- Recorded the Rust-first tool split: keep runtime simulation/rendering in Rust, and allow non-Rust sidecars only for botanical research, registry authoring, or offline analysis when they clearly help
- Marked the current scene/render behavior as aligned with the active contracts, leaving the spatial relation split as a consolidation task rather than a drift bug
- Narrowed the spatial-layer target to a minimal first cut: datum/world transforms, attachment resolution, guide-set lookup, and screen projection helpers
- Added a low-risk extraction sketch for the spatial layer so `coords`, `entity`, `guide`, and `render/guide` can be consolidated in pieces instead of by a big-bang refactor
- Sketched the first canonical spatial API surface with `SpatialPoint`, `SpatialAnchor`, `SpatialAttachment`, `SpatialProjection`, `SpatialGuideIndex`, and `SpatialResolver`
- Mapped the proposed spatial API onto the current modules so the eventual extraction has a concrete mechanical path
- Added a stepwise migration order for the spatial consolidation so the new layer can land behind existing tests and shims
- Landed the first `core/spatial` cut in code and updated the docs to describe it as the active canonical relation layer first pass
- Marked the spatial audit as done for now: behavior matches the contracts, and the remaining work is a planned consolidation into `core/spatial`
- Recorded a preliminary weak-spot audit note: `Space::Anchor` currently resolves like world space, and the projection API still uses `WorldPos` for both world and screen results
- Clarified that the Ghostty `124x32` window size is also the YAM boot/start frame size when the app opens
- Updated the front door and docs index so they both say YAM boots in the `124x32` Ghostty starting frame

## 2026-05-02

- Narrowed the guide contract to linework-only primitives for now, keeping sprites and solid masks explicitly future work
- Folded the soft line reference into the guide contract so linework now has an explicit slope-aware glyph grammar for points, rulers, vectors, streams, and curves
- Added a temporary soft-line probe to the debug overlay so the new linework grammar can be tested in-world at specific coordinates
- Upgraded the soft-line probe to use a punctuation ramp so shallow strokes read closer to the ASCII linework reference instead of flattening to `.` and `-`
- Extended the soft-line probe into a broader punctuation ramp so linework can express gentler stroke transitions instead of only dot/dash traces
- Tightened the soft-line probe back to a sparse reference-shaped ramp so the current probe matches the desired `. _ - '`-style line profile more closely
- Added a soft-line atlas document for the 10x1..10x5 direction families so linework can stay directionally unambiguous during future guide/vine work
- Extended the soft-line atlas with longer slope families so the guide engine has a documented path for world-spanning lines, not only short primitives
- Reframed the line generator as a project-wide guide/mask tool with a Bresenham geometry layer and glyph-appearance layer, not only a vines helper
- Extracted the shared soft-line rasterizer into `core::guide_line` and wired the debug guide visualization path through it so guide rendering and the debug probe use one line engine
- Ingested the first ASCII Draw Studio sample sets and folded their inferred long-line cadence into `docs/soft-line-atlas.md` as a sample-derived grammar note
- Cross-referenced `filled_soft_line_slope_form_v0_1.md` alongside the straight-primitive samples in `docs/soft-line-atlas.md` so the inferred grammar points at both study sources
- Added a first-pass inferred bucket table to `docs/soft-line-atlas.md` so the sample-backed line grammar is recorded as explicit family/cadence hypotheses
- Updated the soft-line contracts to state the real target plainly: the guide engine must generate any line in any direction across the full YAM world size
- Added a v1 grammar key to the soft-line atlas so the line engine selection order is explicit: `LineFamily -> LengthBucket -> Direction -> PhaseRole -> LocalStep`
- Added explicit Rust-side line grammar types in `core::guide_line` so the atlas key now has a code-facing classifier and dispatch path
- Replaced the line glyph branch tree with a literal atlas table in `core::guide_line` so the grammar is editable as data instead of only as match logic
- Expanded the soft-line atlas notes with explicit calibration buckets for `10x2`, `10x3`, `10x4`, `10x5`, `7x2`, `8x3`, `9x4`, and `6x5` so the longer sample families stay visible in the contract
- Retargeted the debug soft-line probe to a long shallow calibration line so the visible test line now exercises the sampled long-span cadence more directly
- Restored the debug soft-line probe to the canonical mirrored `64x10` calibration span `(-28, 22) -> (36, 12)` so the probe matches the manual sample target again
- Tuned the `64x10` long-shallow glyph cadence toward the canonical `--''` / `__. -` reference so the probe stops collapsing into mostly underscores
- Documented the remaining `64x10` smooth-line fidelity gap in the soft-line atlas and render contract so the long-shallow grammar issue stays explicit while the engine is still being tuned
- Added a cadence-driven long-shallow fallback in `core::guide_line` so the canonical `64x10` probe can express a clearer entry / ramp / core / exit rhythm before the local-step fallback takes over
- Added the mirrored `64x10` long-shallow calibration probe in the debug surface so both directions can be compared side by side during cadence tuning
- Made long-shallow punctuation slope-sign aware so the glyph lean can match cell directionality instead of reading as generic filler punctuation
- Added a coarse `CellBand` to the line grammar key so glyph choice can also reflect top / middle / bottom placement inside the terminal cell, not only slope and cadence
- Exposed a soft-band readout in the debug panel for the canonical probe so the new `CellBand` classifier can be inspected while line cadence tuning continues
- Switched `CellBand` classification to use the stroke’s sub-cell position relative to the ideal segment so glyph placement can reflect where the line actually sits inside the cell
- Documented the remaining `64x10` failure modes after the latest screenshot pass: segmented silhouette, weak band/glyph correlation, and insufficient resemblance to the manual reference despite the new cadence and band scaffolding
- Added explicit labels and optional groups to `Guide` so guide primitives can be addressed individually and as part of larger named sets, which shifts the guide contract toward composable guide/mask groups instead of anonymous strokes
- Added `GuideSet` to `GuideState` so named guide collections can be stored and queried explicitly instead of being inferred only through per-guide groups
- Added `GuideSet::new(...)` and `GuideState::add_set(...)` so named guide collections have a small construction API instead of requiring direct vector mutation

## 2026-05-01

- Unified the hotkeys/move/settings popup shell so the modal family shares one centered BTAS-style backdrop, border, and sizing contract
- Moved the camera/world debug scrollbars from the inset gauge track to the outermost terminal row/column so the indicator frame now hugs the screen edge
- Cut release `0.3.1` after the modal-shell, camera, debug-overlay, and hero-rendering polish pass
- Pruned the vines backlog wording so `TODO.md` stays execution-only and the vines contract stays in the owning docs
- Made `dev_mode` a stronger umbrella by closing hotkeys, move, and settings modal state when dev mode turns off
- Added a pre-new-feature gate to hygiene and TODO so future feature work starts from a clean modal/camera/hero/doc/test baseline
- Split the dev-mode camera hotkey into `C` store / `c` recall so the manual camera home can be saved and restored explicitly
- Added a dev-only blinking pointer probe that moves with arrow keys and reports its absolute world position in the debug panel, with footer and hotkeys entry points
- Added world-space guide primitives to `WorldState` as the first guide/coordinates layer for future vines, with docs updated to keep them distinct from raster masks

## 2026-04-26

- added `docs/SCENE_MODEL.md` to formalize the deterministic scene layer above ratatui, including layer order, coordinate spaces, masking, and the frame pipeline
- aligned the scene docs with the terminal presentation split: world, HUD, and overlay
- compiled the `new/` concept notes into the repo-wide `TODO.md` backlog at the repository root
- tightened `TODO.md` wording so it matches the scene and architecture docs without introducing a second projection or presentation vocabulary
- added `docs/REFERENCE_ARCHIVE.md` to catalog the `yam-rust_debugging_260424-2009_dump` notes as reference-only archive material
- marked the archived dump notes as reference-only in `TODO.md` so the active backlog stays separate from historical context
- added an archive-derived ideas section to `TODO.md` so the useful design patterns stay visible without collapsing the archive back into active work
- added a concrete next-steps section to `TODO.md` for projection, invariance, and hero-rendering follow-up work
- replaced the TODO implementation order with the explicit work order: ui -> main scene stabilisation -> hero gif -> main scene stabilisation -> main scene vines -> main scene stabilisation
- expanded the `ui` phase in `TODO.md` with a checklist that keeps projection ownership, world-state boundaries, and footer/HUD/overlay responsibilities explicit
- added a main scene stabilisation checklist to `TODO.md` covering singular projection, world/HUD boundaries, and resize invariance before hero work resumes
- added a hero GIF checklist to `TODO.md` that keeps frame ownership, geometry stability, and cached rendering decisions explicit before the vines phase
- added explicit main scene stabilisation exit criteria to `TODO.md` so resize, camera, footer, and border-probe behavior have a hard checkpoint before hero or vines work continues
- added a main scene vines checklist to `TODO.md` that keeps vine work world-attached and constrained by the same projection and masking rules as the rest of the scene
- clarified that every stabilization checkpoint in `TODO.md` must end with the scene matching the current presentation contract before the next phase starts
- added a final TODO alignment rule so the backlog stays subordinate to `SCENE_MODEL.md` and `ARCHITECTURE.md` whenever the work order changes
- archived the `todo_review.md` critique and promoted its strongest gaps into `TODO.md` as explicit projection, invariants, RenderState, validation, and hero decision-gate contracts
- promoted archive ideas into `TODO.md` as explicit projection, layering, determinism, and greenhouse integration contracts
- promoted archive stress-test ideas into `TODO.md` as startup invariants, projection round-trip checks, and static-input determinism validation
- moved dated audit/update/issue reports into `docs/archive/reports/` and added an archive index so active docs stay contract-focused
- demoted `NOTES.md` into a short working-notes file and archived the old `DOTFILES_MIGRATION.md` report with the other dated docs
- clarified the front-door docs split: root `README.md` for orientation, `docs/README.md` for navigation, and `NOTES.md` for non-authoritative working notes
- documented the filename convention that reserves uppercase markdown names for high-visibility entry points and prefers lower-case names for routine docs
- renamed the routine contract docs in `docs/` to lowercase filenames so the tree reserves uppercase names for the few front-door entry points
- flattened the root `README.md` into the master repo doc so it now owns the active backlog and working guidance while `TODO.md` and `NOTES.md` act as compatibility pointers
- removed the root `TODO.md` and `NOTES.md` stubs after folding their useful content into the master `README.md`
- restored `TODO.md` as the active backlog, trimmed `README.md` back toward a front door, and repaired stale docs links to the lowercase contract filenames
- rewrote `docs/audit.md` into a current risk snapshot, trimmed `TODO.md` back to backlog pointers, and archived the completed flattening/version-map notes
- documented `scene_config.json` in `docs/config.md` as the repo-tracked default scene configuration surface
- standardized the conceptual scene naming from Ivy to vines in `docs/scene-model.md` so the active docs use one term consistently

## 2026-04-24

- isolated legacy Python runtime, render, engine, hero, morphology, shape, theme, and UI artifacts under `tools/legacy-python/`
- moved experimental Python entrypoints under `tools/experiments/`
- normalized `src/theme/` into palette/style/glyph-oriented styling modules
- moved rendering authority from `ui` toward `scene` layers and removed legacy panel rendering from the active path
- added fixed-size hero frame normalization and control-character cleanup to reduce frame jitter
- introduced grid-backed composition primitives, scene-level `LayerOutput`, mask plumbing, and a hero mask probe path
- added persisted runtime UI state for hero offsets, clock offsets, clock font, camera position, and hero animation FPS
- added debug-only editing controls, debug telemetry, and split normal/debug footer text
- added `scripts/update.sh` and CLI update/check-update entrypoints
- added build identity plumbing for runtime footer/version reporting
- documented current coordinate/camera/rendering caveats in the repo audit and rendering docs
- standardized the active camera/viewport path so fullscreen can lock to a datum-centered crop while windowed mode keeps mutable panning, and aligned the debug border mapping to it
- removed render-time hero/clock anchor writes from the active layer path and made debug reconstruct its telemetry from pure state helpers
- removed the legacy `Layer::render(...)` path from the scene contract and dropped the `(0,0)` hero sentinel from runtime state
- made the grid writer grapheme/display-width aware for safer text placement
- verified the tree with `cargo check` and `cargo test` during the audit pass
- removed the centered viewport-tier placement path from `Scene::render` so resize/fullscreen transitions no longer introduce a second framing rule
- added a read-only per-frame `FrameContext` so hero, clock, and debug now share one projection snapshot instead of recomputing it inside layers
- added coordinate invariance tests for anchor/world/screen composition and screen-space overlays
- removed stale repo-split wording after deleting the legacy `/Users/maciejkuster/_git/yam` checkout
- ingested the 2026-04-24 chatgpt audit report and folded its findings into the repo audit trail
- renamed the per-frame snapshot to `RenderState` so the render path has one explicit read-only frame contract
- aligned repo docs with the `RenderState` snapshot contract and removed stale `UiState` side-effect wording
- corrected the active hero/clock placement contract so both remain world-pinned and camera movement no longer reprojects them
- tightened the world datum contract to define `(0, 0)` as the centered origin with signed quadrants around it
- recorded the coordinate orientation split: world is Cartesian, terminal/screen is downward-y
- recorded the hero geometry contract: `820x820` source GIF renders to a fixed `96x48` terminal footprint
- replaced the dotted debug world-edge probe with a stable ASCII frame border probe
- documented the intentional debug border padding row and side padding cell for future UI placement
- documented that the bottom one-row padding is currently occupied by the footer
- clarified that camera is the world-space origin of the crop and viewport is the terminal-sized crop rectangle
- recorded the world-ui vs hud-ui split: world-ui stays attached to world entities; hud-ui stays attached to the viewport/camera/terminal frame
- clarified that world-ui stays world-pinned while hud-ui stays screen-attached, and encoded that split in `src/scene/coords.rs`
- reclassified the clock as world-ui and the footer/status bar as hud-ui so the active contract matches runtime behavior
- split `RenderState` into explicit `world` and `hud` sections and added a resize-invariance test for the frame builder
- encoded the footer bottom-row rule in `footer_row(height)` and added a unit test for the HUD row contract
- restored the ASCII world border probe as a datum-centered world-space indicator and aligned its camera-projected meaning with the active contract
- added a current-issues report covering the remaining static-vs-dynamic contract confusion points
- tightened the current-issues report with an explicit static/dynamic placement table for world-ui, hud-ui, camera, and viewport semantics
- clarified the fullscreen lock contract in the current issues, audit, and rendering docs so windowed panning and fullscreen datum-centered static framing are distinct rules
- implemented the fullscreen lock as a render-state rule and added tests that pin fullscreen datum-centered lock vs windowed-pan behavior
- fixed the current-issues note to call out the debug telemetry mismatch where `Clock screen` was still reported from raw world data instead of the projected value
- added `RenderState::clock_screen()` so clock rendering and debug telemetry share one projected clock position
- added tests for projected clock telemetry and fullscreen clock-screen invariance under stored camera motion
- updated the current-issues and rendering docs to mark the clock telemetry mismatch corrected
- changed the default windowed starting camera baseline to `(-69, -17)` for the `124x32` terminal screenshots
- clamped windowed runtime and render-state camera crops to at most one cell of overscan beyond the world border/frame and recorded that architecture rule
- added tests for the `124x32` starting baseline and windowed camera overscan clamp
- expanded and flattened decoded GIF subimage frames to an opaque full `820x820` logical canvas before chafa rendering to prevent frame 15 and frame 30 vertical overstretch
- added a regression test for full-canvas opaque hero frame geometry across partial GIF frames
- hardened the active docs with explicit assertions, out-of-scope sections, and a docs-map "where to change things" guide so the contract docs are easier to keep single-purpose
- sharpened the active docs with more testable assertions, stricter projection ownership wording, and verification tags in the backlog so the docs read more like guardrails than commentary
- tightened the audit/config wording around `scene_config.json` and added a Rust regression test that pins the tracked defaults to the current runtime scene surface
- added a shared `RenderState` projection consistency test and narrowed the audit wording for the remaining projection/camera risk
- added a resize round-trip invariant at the frame-builder boundary and updated the audit to reflect the tighter projection/camera coverage
- extracted the debug border probe bounds into a tested helper and downgraded the footer/debug sync risk in the audit
- marked the debug border/footer sync risk resolved after adding a direct bounds test for the border probe helper
- added a shared projection-helper test that checks hero and clock screen placement against the viewport origin
- documented the hero path as the active chafa-backed baseline and removed the open decision-gate wording from the audit
- removed the last open-ended hero migration phrasing from `TODO.md` so the backlog matches the current chafa-backed baseline
- tightened the docs hierarchy by moving archive entry points out of the active index and trimming backlog/archive digest text from `TODO.md`
- added explicit config authority wording, a repo hygiene behavior-change rule, environment assumptions, and a layer-order assertion for the active renderer
- added change-impact hints to the active contract docs, a deterministic full-frame render check, and a terminology guardrail in hygiene
- added a zero-size viewport guardrail test in `RenderState` so projection helpers explicitly reject empty screen areas
- added further-development guidelines to `TODO.md` so new backlog items stay execution-only and continue to point at the owning contract docs
- clarified in `docs/rendering.md` and `TODO.md` that all scene rendering continues through the existing `render_scene` / `Scene::render` pipeline
- added a mask-semantics test proving the hero mask only blocks the field layer, and tightened the corresponding wording in the render and scene-model contracts
- added a negative mask-semantics test proving the hero mask does not block non-field overlay layers
- reinforced the scene layer mask contract with a direct overlay-layer negative test in `src/scene/mod.rs`
- extracted explicit `scene::entity::hero_and_clock_poses(...)` plus smaller pose helpers for attachment math and updated the architecture and backlog docs to point at them
- made chafa temp frame directories unique per render batch to avoid parallel-test collisions while preserving the existing hero rendering contract
- reran the render suite after the temp-dir fix and confirmed the chafa hero tests still pass under parallel execution
- added owned cleanup for chafa temp frame directories and a regression test proving each temporary render batch is removed after use
- added backlog guidance for negative tests and run-isolated temp/shared-state artifacts, plus a matching hygiene rule
- added a boundary-change guideline for explicit negative tests in hygiene and the active backlog
- wrapped hero and clock attachment math in a named `scene::entity::HeroClockAttachment` to make the extraction seam more explicit and keep the hero/clock pairing as one attachment object
- moved hero and clock attachment construction behind a `UiState` accessor so the frame builder reads one attachment source of truth
- added a `UiState::hero_clock_attachment()` test and documented `UiState` as the runtime source of truth for hero, clock, and camera attachment inputs
- added a second `UiState::hero_clock_attachment()` test to prove offset changes flow through the attachment object as expected
- split the debug/clock control flags into a small `UiState::meta` substate, added a regression test proving metamechanics toggles do not affect attachment facts, and documented the subordinate metamechanics boundary in architecture/rendering/TODO
- persisted the metamechanics toggles alongside the existing UI state bundle so debug/layout and anchored-clock preferences now survive reloads, with a snapshot roundtrip test covering the new state shape
- added a legacy offsets-only load path test so the persisted UI snapshot still accepts old state files while defaulting metamechanics flags to false
- gave `anchored_clock` a concrete presentation meaning: it now switches the clock between a world-attached hero companion and a screen-attached HUD fallback, with a regression test covering both modes
- renamed the umbrella metamechanics toggle from `debug_layout` to `dev_mode` so the control surface reads as editing/layout mode while `debug` stays the diagnostic surface
- updated the footer help to show `[d]ev` as the visible hint for the `dev_mode` hotkey
- added a frame-level regression test that pins the `[d]ev` footer hint in rendered output so the mode label is covered at the buffer/snapshot boundary
- added a modal tabbed settings popup in the overlay layer, with `dev_mode` gating and tab sections for positions, widgets, gif, and theme values
- inserted the settings popup between debug and footer in the active layer order and updated the layer-order contract to include its new `z_index = 400` tier
- restored `UiState::hero_clock_attachment()` to the shared `hero_and_clock_poses(...)` helper so the attachment seam remains live and `cargo install` stays warning-free
- corrected the clock contract back to a single world-attached hero entity and removed the temporary HUD fallback path so the debug/info panels now describe the real attachment behavior
- removed the obsolete clock fallback toggle and updated the settings popup, docs, and clock layer so the clock is treated as a world entity only
- documented the clock as a world-attached hero entity in the active scene/render/architecture contracts so debug info and settings text no longer imply a screen-attached UI clock
- cleaned the default footer help so the main scene now shows a compact `[q]uit • [d]ev mode` hint with a right-aligned version stamp instead of the longer play/pause/step banner
- kept the dev-mode footer on the same compact punctuation grammar as the default footer while preserving the richer runtime controls
- removed the redundant loaded-frame-count print from hero loading so the frame count stays visible only in the debug/info panel
- highlighted the footer row as a full-width soft green bar with dark inverse text so the main scene footer reads as a proper bar instead of plain text
- added passive camera/world scrollbar indicators to the dev-mode debug overlay at the third cell inward from the terminal edge, derived from `RenderState` and rendered read-only with ratatui scrollbars
- corrected the scrollbar positioning math so the passive camera/world indicators track the actual viewport origin more faithfully, and recolored them dark blue to separate them from the footer bar
- refined the scrollbar diagnostics to derive their size and placement from the visible crop so the bars describe camera/world position rather than reading like a scrollable panel
- kept the passive camera/world indicators ratio-based and read-only while loosening the regression to match the actual ratatui thumb placement instead of a single hard-coded cell
- remapped the passive camera/world indicators to use the world-min camera offset as scroll position so the four corner positions line up with the world extents more naturally
- normalized the passive camera/world scrollbar indicators across the full camera range so the top-left and bottom-right edge cases align with the scrollbar ends more faithfully
- simplified the passive camera/world indicators into a minimal gauge look with dot tracks and bold blue thumbs to make the dev overlay cleaner without changing the diagnostic mapping
- softened the horizontal camera indicator thumb so the gauge reads lighter while the vertical bar keeps its stronger weight
- switched the camera/world gauge glyphs to a thinner `┄`/`═` horizontal pair and `┊`/`║` vertical pair for a cleaner diagnostic look
- changed the default camera path so follow-hero mode stays centered on the world datum across terminal resizes instead of keeping the old top-left-leaning windowed baseline
- surfaced the current camera mode in the debug/info panel and settings widgets so resize checks can distinguish follow-hero from manual pan immediately
- added a `[c]amera` dev-mode hotkey that switches the camera back into centered follow-hero mode and surfaced that control in the footer and active contracts
- split the dev-mode footer controls into a compact `[h]otkeys` popup, keeping the footer itself to the short quit/dev hint while the popup carries the longer control list
- added a dedicated hotkeys popup at `z_index = 390` and removed the long inline dev-mode footer controls so the footer stays compact while the modal lists current developer controls
- added a dedicated move popup at `z_index = 395` with `1/2/3` target selection and `hjkl` movement, keeping entity editing explicit and out of the footer
- simplified the default debug info panel to a smaller live-facts layout and moved the attachment derivation chain out of the default surface
- added a hero-visible fact to the compact debug panel so the projected anchor can be distinguished from the actual in-frame footprint
- changed the startup camera seed to manual pan so boot uses the balanced hero/clock composition instead of defaulting into follow-hero mode
- retuned the manual boot camera seed to `(26, -5)` so the hero and clock both start inside the frame with a balanced composition
- retuned the manual boot camera seed to `(-63, -17)` so the hero and clock start in the screenshot-aligned boot composition the repo now treats as default
- documented the split between the screenshot-aligned manual boot seed `(-63, -17)` and the centered `follow-hero` runtime path so boot composition and resize behavior stay separately owned
- added opaque BTAS-style backdrops to the move and settings popups so modal controls stay readable over the world
- treated background-colored space cells as opaque compositor writes so modal popups clear the hero GIF instead of letting it bleed through the panel surface
- documented a lighter verification rule: docs-only and wording-only changes use `cargo fmt --check`, while compositor/camera/overlay changes keep the full test suite
- added a compact UI/metamechanics working-set snapshot to the active contracts so future UI work can resume from the current modal/camera split without rereading the full history
- added a small hero-frame tone lift for low-luma reds before chafa conversion so dark reds stay visible in terminal output
- preserved the styled Chafa spans when copying hero frames into the terminal buffer so the hero no longer collapses to monochrome text
- updated `hero_layer` so it preserves Chafa span styling when copying hero frames into the scene grid, keeping the hero colorized instead of flattening it to plain text
- tightened the hero tone lift so dark reds stay visible without over-lifting the rest of the palette
- remapped low-luma reds toward a fixed red anchor so the original red family survives Chafa without shifting the other colors
- replaced the red anchor remap with a tight hue/saturation/value gate so only genuinely dark reds get a visibility lift while skin and orange tones stay unchanged
- switched the hero Chafa configuration to `--color-space=rgb`, `--color-extractor=average`, and `--dither=none` as the first quantization experiment for recovering dark reds before any pixel-side correction
- split the hero pipeline into braille glyph generation plus per-cell source color sampling so dark-red cells can survive without broad palette remapping
- documented the ditherit-style hero rendering trial as a partial success: it preserves more dark reds, but the braille-cell sampling also smudges the face and edges enough that the strategy remains experimental rather than final
- rolled the hero renderer back to the earlier Chafa `rgb/average/none` path after the ditherit-style braille/source-color trial proved too blocky in the face area, while keeping the trial itself documented as historical context
- recorded `LachlanArthur/Braille-ASCII-Art` as a future hero-renderer reference for explicit `2x4` braille dot packing and error-diffusion comparison without changing the active Chafa-backed baseline
- documented the desired hero-rendering direction as an offline compiler pipeline that separates monochrome braille shape decisions from color assignment, supports curated pre-generated frame corrections, and emits a stable `HeroFrameSet` cache for runtime use
- incorporated the ANSI-tooling research conclusion that YAM needs a structured per-cell `CellGrid` correction layer between ANSI/converter output and the final `HeroFrameSet`, because existing editors do not cover Unicode braille, truecolor, animation, and surgical cell edits together
- documented the REXPaint/CrossOver path as an optional `.xp` manual-editing experiment behind `CellGrid`, with braille font/tile mapping and round-trip validation required before edited frames can feed `HeroFrameSet`
- documented the Ansizalizer/ansipx trial as a failed Chafa-replacement direction: truecolor output must be converted to 256-color for Durdraw, and custom/full-braille glyph ramps produce density texture rather than the `2x4` dot-mask geometry needed for hero face and silhouette fidelity
- repurposed the dev-mode `c` hotkey from follow-hero to camera reset, so it restores the screenshot-aligned manual boot seed `(-63, -17)` for the default scene
- added a soft feature-freeze rule to hygiene and the active backlog so future work stays in polish/stability mode unless a bug or contract violation justifies new behavior
- added a hero render regression test that exercises the buffer write path with styled spans so future hero rendering changes cannot silently flatten colorized output again

## 2026-04-23

- pruned Go-era root artifacts from the repo
- moved the active documentation surface to `docs/`
- removed the legacy `docs/v2/` tree
- updated the root README and hygiene notes for the Rust baseline
- committed the Rust baseline and the documentation hygiene refresh
- removed dead `visualizer/` ignore entries and normalized the repo hygiene wording
- verified `cargo clippy -- -D warnings` and `cargo run -- --version` on the Rust baseline
- added architecture contract docs, module headers, and a strict repo check script
- enforced the module boundary contract and verified `scripts/check.sh`
- added the render layer contract and scene-level render order header
- integrated a cached chafa-backed hero frame path with escape-sequence stripping
- switched the hero frame path to ANSI-preserving ratatui text and derived hero bounds from the rendered frame size
- replaced the hero snapshot path with a persistent chafa stream and UI-owned frame updates
- added a compile-time/runtime identity stamp to prove `cargo run` and `yam-rust` share the same source tree and build markers
- removed the chafa stream signature gate so frame snapshots are no longer collapsed before reaching the hero state
- aligned the crate/package version with the runtime footer so `cargo run` and `yam-rust` both report `v0.3.0`
- replaced PTY-based hero capture with deterministic GIF decoding plus per-frame single-image chafa rendering
- fixed temp GIF frame export to deterministic PNG filenames and verified the hero buffer loads 64 frames in tests
- preserved ANSI-derived style spans while clipping hero frame text in the renderer
- introduced a minimal scene + layer compositor and routed the existing render order through it without changing output
- `2026-05-13 10:12` finished the companion-cluster polish pass: froze the accepted default camera, hero, and clock launch positions; restored clean-launch reseeding so saved offsets cannot override those defaults; kept manual camera centered through terminal resizes; brought the date widget back into the visible companion band and exposed its world/screen/visible facts in the debug panel; nudged the default weather/date positions into the accepted final family; and restored the spaced compact temperature row (` 3C |  11C`) with a slightly wider compact weather contract and matching layout-test updates.

## 2026-06-04 14:03 CEST

- backed away from the earlier front-door README repair pass and returned the README closer to its lighter pre-repair posture while keeping the GitHub-hosted preview GIF in place

## 2026-06-04 14:18 CEST

- reshaped the front-door README for tighter Codex readability and better cross-doc coherence: removed the duplicated release marker, centered the preview GIF and badges, kept the short personal intro, and collapsed the rest of the file into a compact repo-orientation sheet with current state, commands, launcher behavior, canonical docs, repo shape, and working rules aligned to the active contracts

## 2026-06-04 14:29 CEST

<!-- cspell:ignore Dini Timm twimc -->

- added a README-local `cspell` ignore for `Dini`, `Timm`, and `twimc` so the restored intro can stay intact without weakening broader docs hygiene
- added one bounded backlog note for future external eval report ingestion: raw reports stay reference input, active findings get triaged into `TODO.md` or `docs/audit.md`, and each ingestion batch must be recorded in `docs/LOG.md` instead of creating a second authority surface

## 2026-06-04 14:32 CEST

- ingested the first external README eval as reference input rather than a new authority doc: recorded the active actionable residue in `TODO.md` as future bounded README polish and added a matching low-stakes audit note that the front-door direction is correct but still has minor hierarchy, labeling, and claim-precision seams

## 2026-06-04 14:59 CEST

<!-- cspell:ignore isthatayam -->

- replaced the README's remote GitHub-hosted preview GIF with the local repo-owned `assets/isthatayam_github.mp4` clip, added a direct file fallback link beneath the centered media block, and updated the intro wording from animated `gif` to animated `clip`

## 2026-06-04 17:03 CEST

- expanded the README-local `cspell` ignore list after the latest browser-side README rewrite so the pushed acknowledgements and restored `twimc` heading stay compatible with the docs gate without weakening broader repo spelling checks

## 2026-06-05 08:54 CEST

- noted the latest hand-reworked README as the current good-enough creative/front-door state for YAM: the intro, front GIF, compact orientation-sheet structure, and acknowledgements are now considered broadly settled in tone and direction, so future README work should stay light unless a factual or hygiene issue requires intervention

## 2026-06-05 14:13 CEST

- relaxed `scripts/check-docs.sh` so the README version sync check now accepts both `Current release:` and `current release:` forms instead of tripping on the hand-maintained lowercase variant every time; the checker still enforces presence and version parity with `Cargo.toml`

## 2026-05-13 - Audit cleanup follow-up

- refreshed `docs/audit.md` review freshness after the companion-layout stabilization pass
- updated `TODO.md`'s immediate batch so it reflects actually pending audit-follow-up work instead of already-completed doc cleanup
- renamed the debug overlay's `Hero visible` line to `Hero anchor visible` because the current fact measures projected anchor visibility, not full rendered hero-pixel visibility

## 2026-05-13 - Release 0.3.6

- bumped the crate and runtime release marker from `0.3.5` to `0.3.6`
- cut the patch release after the companion-cluster stabilization work: default camera, hero, clock, weather, and date launch positions now follow the accepted clean-boot composition more reliably
- carried forward the clean-launch reseeding fix, manual camera resize recentering fix, restored spaced weather temperature formatting, date visibility/debug-fact repair, and the debug wording cleanup that clarifies `Hero anchor visible`

## 2026-05-13 - Visual companion nudge

- nudged the weather companion one cell left relative to the frozen clock anchor
- applied a matching one-cell left bias to the date line's self-centering so the date and weather pair stay visually grouped against the clock
