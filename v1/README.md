# YAM v1

This directory marks the current baseline implementation.

## Current Baseline

- `visualizer/` for the standalone terminal scene
- `ghostty/`, `fastfetch/`, and `bin/` for the shell startup path
- `docs/` for the current operational maps

## Rule

Treat v1 as the compatibility baseline until v2 replaces specific behavior.

Do not move or rewrite the v1 stack casually.
